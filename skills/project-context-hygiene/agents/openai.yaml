interface:
  display_name: "Project Context Hygiene"
  short_description: "Keep GitHub-native project memory clean and durable"
  default_prompt: "Use $project-context-hygiene to reconcile GitHub Issues/Project state with durable repository context, validate archives, and remove stale mutable Markdown queues without losing history."

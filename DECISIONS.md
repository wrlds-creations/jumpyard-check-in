# Decisions

This file is the source of truth for meaningful project decisions. Add entries when a choice affects architecture, scope, cost, data ownership, security, deployment, UX direction, or future maintainability.

## Decision Log

| ID | Date | Decision | Rationale | Impact | Revisit Trigger |
|---|---|---|---|---|---|
| `D0001` | 2026-05-18 | Frontend must not call Roller directly in production architecture. | Roller credentials must not be exposed in frontend. A server-side layer is needed for logging, retries, error handling, and controlled fallbacks. JumpYard Cloud/server API will own pilot operational state. | Production architecture uses `check-in app -> JumpYard Cloud/server API -> Roller API`. Frontend work must target server-owned contracts rather than Roller directly. | Revisit only if Roller provides a credential-safe frontend integration model and WRLDS explicitly approves an architecture change. |
| `D0002` | 2026-05-18 | Roller integration must fail closed unless configured for Playground. | Sprint 1 starts against Roller Playground only. The integration should reject missing environment config, non-playground environments, and production/live-looking URLs before any Roller client can be used. | Roller validation blocks unsafe config early and keeps production credentials or endpoints out of local/dev validation. | Revisit when production Roller integration is explicitly scoped and approved. |
| `D0003` | 2026-05-19 | Each ticket should use a dedicated `codex/` branch and `main` should change only through a review/merge step. | Ticket branches keep scope clear, make commits reviewable, and reduce the risk of mixing unrelated local changes into project history. A local commit should not be treated as a `main` update. | Codex creates or switches to a ticket branch before ticket work, stages only ticket files, commits only when explicitly requested, and never pushes directly to `main`. | Revisit only if the WRLDS workflow or repository release process changes. |
| `D0004` | 2026-05-19 | JumpYard Cloud should be built as the AWS server-side boundary for phone check-in flows. | The project needs server-side Roller credentials, token reuse, rate-limit control, idempotency, audit logging, normalized responses, and JumpYard-owned pilot state. | Future implementation should target API Gateway/Lambda, Secrets Manager, Aurora PostgreSQL, S3 raw payload storage, CloudWatch, SQS/EventBridge, and a rate-limit control mechanism before phone UI calls Roller-backed operations. | Revisit only if WRLDS chooses a non-AWS hosting model. |
| `D0005` | 2026-05-19 | Booking lookup should use `GET /bookings/{uniqueId or bookingReference}` as the primary live lookup path. | Roller response v3 confirms Get Detail of a Booking is authoritative for live existing-booking lookup. Playground also accepted booking reference `5001370` at `/bookings/5001370`. | JumpYard Cloud lookup contract should normalize booking detail and use `GET /bookings` search only as fallback when a guest lacks QR/code. | Revisit if Roller changes endpoint behavior or booking references stop resolving through the detail path. |
| `D0006` | 2026-05-19 | Check-in should be modeled as ticket-level redemption, not a booking-level checked-in flag. | Roller documentation and process work identify `POST /redemptions` as the supported check-in action, operating on ticket ids. | JumpYard Cloud must resolve booking references to ticket ids before redeeming and must store internal check-in attempts/audit separately from Roller ticket state. | Revisit if Roller provides a confirmed booking-level self-service check-in API. |
| `D0007` | 2026-05-19 | Existing-booking add-products should create a separate linked add-on booking for the pilot. | Same-booking update plus Roller payment link can send the guest to a separate hosted payment page where return to the JumpYard PWA/check-in flow is not reliable enough for the pilot. A separate add-on booking lets JumpYard keep the PWA flow coherent and link the original booking to the add-on booking in JumpYard Cloud. | JumpYard Cloud should create and store a link between original booking and add-on booking. Same-booking `PUT /bookings/{uniqueId}` plus payment link is deferred, not the primary pilot path. | Revisit only if Roller confirms a reliable payment-link return path and JumpYard explicitly chooses same-booking update UX. |
| `D0008` | 2026-05-19 | JumpYard Cloud should store minimal operational state and Roller ids, not full Roller-owned data. | Roller remains source of truth for bookings, products, payments, and ticket redemption. JumpYard needs audit, handoff, idempotency, and short-lived normalized state without unnecessary PII. | Aurora PostgreSQL should hold Roller snapshot tables, operational state, check-in attempts, handoff sessions, idempotency records, product cache metadata, webhook events, and event logs. | Revisit when compliance or reporting requirements require longer retention or richer local data. |
| `D0009` | 2026-05-19 | JumpYard Cloud should maintain a local booking index from daily Data API seed, booking webhooks, and live REST confirmation. | Morning seed reduces peak-time dependency on live Roller calls, webhooks keep same-day changes fresh, and live lookup confirms check-in-critical state. Roller still remains source of truth. | Future implementation should add a BookingIndex and BookingSeedRun model, webhook idempotency, and reconciliation logic before relying on the phone flow at pilot scale. | Revisit if Roller Data API or webhook access is unavailable for the pilot. |
| `D0010` | 2026-05-19 | Playground test bookings should be created by an internal seed tool, not by a public demo button in the phone check-in flow. | Test-data creation needs fake names/products/statuses without polluting guest UX or normal production flows. A protected server-side seed tool can encode Roller-required booking fields once and stay Playground-only. | Future implementation should add a Playground-only seed script or protected admin action that creates deterministic fake bookings through JumpYard Cloud. | Revisit if Roller provides a better native Playground fixture/reset mechanism. |
| `D0011` | 2026-05-19 | JumpYard Cloud infrastructure should start as a deploy-blocked AWS CDK TypeScript foundation. | The project needs a scalable AWS shape before endpoint implementation, but real AWS creation must wait until account, region, owner, data classification, exportability, and cost metadata are confirmed. | T0004 adds CDK definitions and local synth validation only. Deploys remain blocked until WRLDS metadata and AWS account preflight are completed. | Revisit when first dev deploy metadata is confirmed. |
| `D0012` | 2026-05-19 | Booking index ingestion should use daily seed, booking webhook, and live REST refresh as separate responsibilities. | Data API seed is good for a baseline, webhooks catch same-day changes without polling, and live REST detail is still needed before check-in-critical decisions. Separating the paths keeps the phone flow fast without treating local data as truth. | Future implementation should build separate seed run tracking, webhook intake/enrichment, freshness labels, and live refresh logic. Get attendance must not seed expected guests. | Revisit if Roller Data API or webhook access is not available in the pilot environment. |
| `D0013` | 2026-05-19 | The first real AWS dev deploy should happen immediately after T0005, before schema, seed tooling, and endpoint implementation. | T0004 created the CDK foundation and T0005 defines the ingestion contract. Deploying dev next gives the team a real API/database environment to build against instead of designing more implementation in the abstract. | T0006 should confirm WRLDS metadata and deploy the CDK foundation to dev. T0007 should add Aurora schema/migrations, followed by deterministic Playground seed tooling and lookup implementation. | Revisit if AWS metadata is unavailable or the user chooses to continue local-only development. |
| `D0014` | 2026-05-19 | JumpYard Cloud dev Aurora should use `aurora-postgresql` engine version `16.13` in `eu-north-1`. | The first T0006 deploy attempt failed because AWS RDS in `eu-north-1` does not offer Aurora PostgreSQL `16.3`; AWS reported `16.13` as an available PostgreSQL 16 version. | CDK must synthesize the dev cluster with `16.13` so the deploy can complete in the confirmed WRLDS dev region. | Revisit when upgrading Aurora PostgreSQL or changing AWS region. |
| `D0015` | 2026-05-20 | Aurora schema changes must run through versioned SQL migrations tracked in `jumpyard.schema_migrations`. | Backend tickets need a repeatable database state across dev, staging, and production without manual console edits. A migration table makes applied changes auditable and prevents duplicate execution. | `infra/migrations/` owns SQL migrations, and `infra/scripts/run-migrations.ts` applies them through the Aurora Data API for the approved environment. | Revisit if the project adopts a managed migration framework or changes database platform. |
| `D0016` | 2026-05-20 | Roller Playground seed writes must be explicit, guarded, and based on child/variation product IDs. | Roller create-booking payloads require product variation IDs, and test-data writes must never accidentally target Live. A dry-run-first command with a separate write confirmation makes the seed path useful without weakening the Playground guard. | T0008 seed tooling reads products first, generates deterministic booking payloads, blocks Live/production URLs, and writes only when `--apply` plus `ROLLER_SEED_ALLOW_WRITE=I_UNDERSTAND_THIS_WRITES_PLAYGROUND_BOOKINGS` are both present. | Revisit if Roller provides a native fixture/reset mechanism or if seed creation moves behind an authenticated admin API. |
| `D0017` | 2026-05-20 | The first lookup endpoint should use live Roller booking detail before Aurora index lookup exists. | T0009 needs a real phone-flow lookup before daily seed and webhook ingestion are implemented. Roller remains the booking source of truth, and live detail is the safest first deployed behavior. | `POST /v1/check-in/lookup` reads Secrets Manager/SSM config server-side, calls Roller `GET /bookings/{identifier}`, enriches product names from `/products`, and returns a normalized JumpYard response without raw Roller payloads. Aurora persistence and local-index-first lookup remain future tickets. | Revisit after daily seed and webhook ingestion populate the Aurora booking index. |
| `D0018` | 2026-05-20 | Phone booking lookup should show both ready and unpaid found bookings, but only ready bookings can start check-in. | Unpaid bookings are valid bookings and should be visible to the guest, but the flow must not continue into safety/redeem before payment handling exists. The frontend should consume JumpYard Cloud eligibility rather than duplicate Roller rules. | T0010 maps `ready` to booking summary with an active check-in CTA, maps `payment_required` to booking summary with `Obetald` and a blocked CTA, and keeps stop states for `wrong_date`, `no_redeemable_tickets`, not-found, and service failures. | Revisit when payment and staff-assisted recovery flows are implemented. |
| `D0019` | 2026-05-20 | Booking index ingestion should use initial backfill plus daily Data API modified-date sync. | Roller Data API is read-only, optimized for daily export, and returns records by modified date rather than direct real-time lookup by visit date. A booking for today can have been created weeks earlier, so a one-day daily export alone is not enough to build today's operating list. | JumpYard Cloud should first backfill an approved historical/future window into Aurora, then run daily modified-date upserts, use webhooks for same-day changes, and use REST live refresh before check-in-critical decisions. | Revisit if Roller provides a visit-date Data API export or a different bulk export model is chosen. |
| `D0020` | 2026-05-20 | `/data/bookingitems` should import into Aurora as booking and booking-item snapshots without raw Roller payload storage. | Bookingitems is enough to populate the first booking index baseline, but it can include fields such as booking names and notes that may contain PII. The first import should store only normalized operational fields and safe summaries. | T0012 upserts booking-level rows into `jumpyard.roller_bookings`, item rows into `jumpyard.roller_booking_items`, tracks runs in `jumpyard.booking_seed_runs`, and excludes raw payloads, names, emails, phone numbers, and notes from output/storage. | Revisit if a later retention/PII decision approves raw payload archiving. |
| `D0021` | 2026-05-20 | Product names should come from a cached Roller product catalog, not from booking free-text fields. | Product names are needed for booking display and item normalization, while booking names, comments, and notes can contain guest PII or sensitive free text. | T0013 caches REST `/products` rows in `jumpyard.product_catalog_cache` and enriches `jumpyard.roller_booking_items` with `product_name`, `parent_product_name`, and `parent_product_id`. | Revisit if Roller Data API bookingitems starts returning authoritative product names or product catalog semantics change. |
| `D0022` | 2026-05-20 | Guest email and phone may be stored later only as explicit structured contact fields for scoped check-in/SMS use cases. | Email and phone are expected to be needed for guest communication and check-in flows, but they should not arrive through raw payload or free-text storage. | A later contact-data ticket should define exact fields, masking/hashing, retention, and lookup behavior before importing or exposing contact data. | Revisit when implementing customer/contact ingestion or SMS flows. |
| `D0023` | 2026-05-20 | T0014 stores guest email and phone as structured contact fields plus masked/hash fields, while still excluding names, addresses, notes, and raw payloads. | Email and phone are required for future SMS/check-in readiness, but free-text and broader customer profile data should not be imported without a specific use case. | `jumpyard.guest_profiles` stores `email`, `contact_number`, hashes, masked values, `sms_ready`, source, and safe context from `/data/customers`. Outputs and logs continue to avoid printing full contact values. | Revisit when retention, encryption strategy, SMS sending, or production data policy is finalized. |
| `D0024` | 2026-05-20 | Dev webhook intake should acknowledge fast and store only idempotent metadata until enrichment is scoped. | Roller expects webhook endpoints to acknowledge quickly, retries failures, and does not guarantee event ordering. The safest first implementation is to persist a deduped event signal and defer any slower enrichment or booking snapshot mutation. | T0015 returns HTTP `200` for accepted, duplicate, unauthorized, invalid JSON, and oversized requests; returns HTTP `500` for retryable server failures; stores only normalized metadata plus payload hash in `roller_webhook_events`; and uses a dev token until Roller production verification is confirmed. | Revisit when implementing webhook enrichment, IP allowlisting, official Roller webhook auth verification, or production webhook registration. |
| `D0025` | 2026-05-21 | Lookup should use Aurora first for fresh display records and Roller REST only for missing or unsafe records. | The local booking index now contains enough normalized booking, item, product, ticket, and contact context to avoid a live Roller call for every normal phone lookup. Roller remains source of truth when local state is missing, stale, tombstoned, or unclear. | T0016 reads Aurora first, returns existing response shapes to the phone app, refreshes missing/unsafe records through `GET /bookings/{identifier}`, and upserts refreshed booking/item/ticket data back into Aurora without storing raw Roller payloads. | Revisit before redemption, add-on booking writes, production rollout, or when webhook enrichment makes freshness more precise. |
| `D0026` | 2026-05-21 | Dev booking webhook enrichment should refresh from Roller REST and update Aurora synchronously for the Playground spike. | The project needs to prove that a Roller booking change signal can become a fresh Aurora booking snapshot before adding a separate async worker. Returning HTTP `500` on enrichment failure lets Roller retry, while duplicate failed/received events remain retryable. | T0017 fetches `GET /bookings/{identifier}`, enriches product names best-effort from `/products`, upserts booking/item/ticket snapshots, and marks webhook events `processed`, `pending_enrichment`, or `failed`. Real Roller registration remains separate so headers/body can be confirmed safely. | Revisit before production traffic, when adding SQS/EventBridge async enrichment, or if Roller webhook delivery latency approaches the 10 second timeout. |
| `D0027` | 2026-05-21 | Roller Playground webhook registration should be managed by a guarded script, not manual console steps. | Registration is an external integration setting that must be repeatable, idempotent, Playground-guarded, and safe from duplicate webhook creation. T0018 also confirmed Roller sends the configured token in header `x-roller-apikey`. | `npm --prefix infra run register:webhook:dev` dry-runs current registration, and guarded apply registers booking events to the dev endpoint. The dev Lambda accepts `x-roller-apikey` and normalizes numeric Roller event ids to `Created`, `Updated`, and `Cancelled`. | Revisit before production registration, when replacing the dev shared token with production verification, or if Roller changes webhook auth semantics. |
| `D0028` | 2026-05-21 | Phone lookup should carry source/freshness metadata for verification but keep it hidden from guests by default. | Source/freshness is useful for development, support, and future staff/admin surfaces, but guest-facing check-in should stay focused on the booking decision rather than internal system details. The phone app also needs a safer default date than the old fixed demo value. | T0019 maps JumpYard Cloud `source` into the phone booking model, exposes stable non-visible test attributes on the summary, and uses today's `Europe/Stockholm` date as the default expected date unless explicitly overridden. | Revisit when adding staff/admin diagnostics, venue operating-date service, or production support tooling. |
| `D0029` | 2026-05-21 | The first redeem endpoint should plan and audit server-side redemption, but deployed Roller redemption writes stay disabled. | Ticket redemption is a write-critical action and the dev HTTP API is still public. The project needs to prove booking/ticket resolution, eligibility blocking, idempotency shape, and audit logging before exposing a public endpoint that can consume tickets. | T0020 deploys `POST /v1/check-in/redeem` as a safe planning endpoint that resolves Aurora tickets and records planned/blocked attempts. Roller `POST /redemptions` code exists behind `ENABLE_ROLLER_REDEEM_WRITES=false` and must not be enabled until a later controlled ticket adds auth/session protection and final live refresh rules. | Revisit when implementing controlled Playground redeem execution and phone/staff check-in completion. |
| `D0030` | 2026-05-21 | Confirmed dev redemption writes require a separate dev token and final live Roller refresh. | Redemption consumes tickets in Roller, so a fresh local Aurora snapshot alone is not enough. A separate dev token prevents accidental writes from the public endpoint while still allowing controlled Playground validation. | T0021 enables `POST /redemptions` only after `confirmRedeem=true`, valid dev token, Playground guard, live `GET /bookings/{identifier}` refresh, Aurora upsert, and re-evaluation of eligibility. | Revisit before phone UI redeem wiring, staff session auth, or production rollout. |
| `D0031` | 2026-05-21 | Phone UI must not directly execute Roller redemption; pilot redeem completion must go through a server-owned check-in session plus staff/server confirmation. | T0021 proved that redemption consumes real Roller ticket state. Putting a dev token, production secret, or direct redeem authority in the phone app would make accidental or unauthorized ticket consumption too easy. | The phone flow may start or resume a JumpYard Cloud check-in session, but final redeem remains behind staff/admin or trusted server confirmation. The final path still performs live Roller refresh, eligibility re-check, idempotency, and audit before `POST /redemptions`. | Revisit only after a production-grade guest auth/session model is approved and risk-reviewed. |
| `D0032` | 2026-05-21 | Check-in session start and ready-for-staff are JumpYard Cloud operational actions, not Roller writes. | The phone flow needs a real next step after lookup, but starting a session should not consume tickets or depend on Roller availability. Roller redemption remains a later staff/server-confirmed action. | T0023 stores sessions in Aurora, reads existing booking/ticket snapshots, blocks unsafe contexts, writes audit events, and exposes session endpoints that do not call Roller. | Revisit when staff/admin auth, final redeem from session, or production self-service check-in is scoped. |
| `D0033` | 2026-05-21 | Phone check-in must start or resume a JumpYard Cloud session before leaving the booking summary. | Guest progress needs a server-owned session id before safety, add-ons, payment, handoff, or final staff confirmation can be tied together. | T0024 gates the booking-summary CTA on `POST /v1/check-in/sessions`, stores the returned session in phone flow state, and keeps unpaid/unsafe contexts blocked. | Revisit if a future SMS-token resume endpoint changes how the phone flow restores session progress. |
| `D0034` | 2026-05-21 | Phone safety completion should mark the server-owned session ready for staff, but still not redeem tickets. | The guest needs a clear end state after safety, and staff needs a stable handoff code before the staff/admin surface is built. This is JumpYard Cloud operational state, not a Roller write. | T0025 calls `POST /v1/check-in/sessions/{checkinSessionId}/ready-for-staff` after safety attestation, stores the returned handoff code/status, and shows that code on the confirmation screen. | Revisit when staff/admin handoff detail or staff-confirmed redeem changes the ready-state contract. |
| `D0035` | 2026-05-21 | The first staff handoff surface should be read-only and avoid contact PII. | The admin app needs a real operational queue before final redeem is wired, but the dev API is still public and staff auth is not scoped yet. | T0026 exposes only read-only session/list detail data from Aurora: handoff code, booking reference, status, product/ticket summaries, and selected ticket ids. It does not return guest email/phone and does not call Roller or redeem tickets. | Revisit when staff/admin authentication and staff-confirmed redeem are implemented. |
| `D0036` | 2026-05-21 | Dev staff-confirmed redeem may use a manually entered temporary redeem code until staff auth exists. | T0027 needs an end-to-end staff confirmation path, but production staff authentication is still an open follow-up. Reusing the T0021 dev redeem token keeps Roller writes protected without storing a secret in source, browser environment variables, localStorage, or sessionStorage. | The admin app can post `POST /v1/staff/check-in/sessions/{checkinSessionId}/redeem` with a manually entered temporary code; JumpYard Cloud still performs final Roller refresh, eligibility re-check, idempotency, audit, and session completion server-side. | Revisit before production rollout or as soon as staff/admin authentication is scoped. |
| `D0037` | 2026-05-21 | The guest QR should encode a JumpYard Cloud handoff payload, not Roller data or redeem authority. | Staff needs a fast way to open the exact server-owned check-in session, but the QR must not expose Roller credentials, redeem tokens, or a direct write action. | T0028 uses `JY_HANDOFF:<handoffCode>:<checkinSessionId>` as the QR payload. The admin app can scan or paste it to open the session detail, while final redeem still requires the protected staff-confirmed server path. | Revisit when implementing signed/short-lived handoff tokens or production staff auth. |
| `D0038` | 2026-05-21 | New-booking payment work must first prove whether Roller Playground supports in-app PWA payment before building phone UI. | The preferred guest UX is create booking plus fake/test payment without leaving JumpYard PWA, but Roller may require hosted payment, a specific payment component, fake card setup, or domain allow-listing. | T0030 is a discovery spike before T0031/T0032 implementation. Hosted payment links remain fallback until in-app payment is confirmed impossible or too risky. | Revisit after T0030 confirms Roller payment flow, test cards, and domain/security requirements. |
| `D0039` | 2026-05-21 | Phone resume routing should follow JumpYard Cloud session state, not restart every paid booking flow. | Re-entering a booking after safety or staff completion should not force guests through add-ons and safety again. JumpYard Cloud owns session state, so the phone app should route from the returned session status. | T0029 starts/resumes the server session after paid lookup, routes `ready_for_staff` sessions directly from search to QR confirmation, routes completed/redeemed sessions to already checked-in, and keeps guest-in-progress sessions in the normal guest flow. | Revisit when SMS token restore or production session TTL/cleanup rules are implemented. |
| `D0040` | 2026-05-21 | New-booking payment should use Roller draft booking plus `paymentJwt` as the primary candidate, while implementation waits for Roller payment-library prerequisites. | T0030 confirmed `POST /bookings/draft` works in Playground and returns cost fields plus `paymentJwt`. Roller Payments docs describe a custom-checkout path using Roller's payment library and Adyen drop-in, but require ROLLER approval, public HTTPS domain allowlisting, and an approved payment package. | T0031 can build server-owned quote/draft endpoints, but T0032 phone payment UI must not assume fake/test payment works until package access, test cards, and allowlisting are confirmed. Hosted payment remains a fallback, not the preferred pilot path. | Revisit when Roller enables the payment package/test domain or if Roller says the PWA cannot use the payment library for this venue. |
| `D0041` | 2026-05-22 | Payment-package proof-of-concept should run as a local JumpYard Cloud harness until Roller prerequisites are available. | The real payment drop-in requires an approved Roller package, public HTTPS allowlisting, and test-card instructions. Building phone UI before those inputs are available would hide the actual blocker and risk committing to the wrong UX. | T0032 adds a local POC command that exercises JumpYard Cloud quote/draft endpoints, checks payment-package/public-origin readiness, and keeps all raw `paymentJwt` values out of logs. Phone payment UI remains deferred until the POC reports the prerequisites are ready or a fallback is explicitly chosen. | Revisit when Roller provides the payment package, allowlists a public test origin, and confirms fake/test card behavior. |
| `D0042` | 2026-05-22 | T0033 should build the phone pre-payment booking flow while deferring payment drop-in to a later ticket. | Roller payment execution is blocked externally, but product/time selection, availability/capacity check, quote, and guarded draft creation can be built now against JumpYard Cloud. This keeps progress moving without guessing payment-package behavior. | T0033 should include server-side availability/capacity checks for session/capacity products before quote/draft where needed. The phone app must still call JumpYard Cloud only, show a payment-pending state, and must not collect card details or render the payment package until a dedicated payment ticket. | Revisit when Roller payment package, allowlist, and test-card details are available. |
| `D0043` | 2026-05-22 | Raw Roller payment JWTs must stay response-only, while JumpYard Cloud stores safe pre-payment draft state. | T0033 needs durable operational state for draft follow-up, support, and future payment completion, but a `paymentJwt` is a payment-session credential and should not be stored in Aurora or logs. | `jumpyard.prepayment_booking_drafts` stores Roller draft ids, idempotency keys, status, date/time, selected item summaries, totals, guest email/phone plus masked/hash fields, JWT presence, and payment-config availability. It does not store the raw `paymentJwt`; the phone flow stops at payment pending until payment execution is explicitly scoped. | Revisit only if Roller requires server-side JWT replay for payment completion and a security-reviewed storage strategy is approved. |
| `D0044` | 2026-05-22 | Add-product step 1 should create a separate linked add-on draft booking, not mutate the original booking. | This preserves the selected D0007 pilot UX, keeps the guest inside the JumpYard PWA path, and avoids `PUT /bookings/{uniqueId}` plus hosted payment-link return-risk while payment execution remains blocked. | T0034 exposes server-side add-product quote and draft endpoints. Quote calls Roller draft costs without creating a booking. Draft creates a separate Roller Playground draft booking, persists `flow_type='add_product'` in `jumpyard.prepayment_booking_drafts`, links it through `jumpyard.booking_links`, and leaves raw `paymentJwt` response-only. | Revisit only if Roller confirms a reliable same-booking payment return path and JumpYard explicitly chooses same-booking update UX. |
| `D0045` | 2026-05-22 | Phone add-product drafts may be stock-only and must stop at payment pending. | Existing-booking add-ons can be products such as socks or padlocks that do not create redeemable jump tickets. Treating those drafts as normal check-in bookings would confuse staff handoff and redemption. Payment execution is still blocked by Roller prerequisites. | T0035 phone UI creates separate linked add-on drafts only after server-side quote and contact capture, disables unmapped add-ons, stops at payment pending, and does not route stock-only drafts into safety, QR, or redeem flows. | Revisit when payment completion and post-payment add-on fulfillment are scoped. |
| `D0046` | 2026-05-22 | Data API and SMS readiness should proceed before payment drop-in while Roller payment prerequisites are blocked. | The in-app payment package still depends on external Roller/Pabel inputs, but full Data API backfill, daily sync, and SMS session links can move the check-in platform forward without guessing payment behavior. | T0036-T0039 focus on Data API import/scheduling and SMS token/sending foundations. Roller payment package/drop-in moves to T0040 and remains blocked until package access, allowlisted HTTPS origin, and fake/test card details are available. | Revisit immediately when Roller/Pabel provides the payment prerequisites or if SMS timing becomes lower priority than payment. |
| `D0047` | 2026-05-22 | Dev daily Data API sync should run as EventBridge -> Lambda and upsert the same Aurora snapshot tables as local backfill. | The local T0036 orchestrator proved the source set, but dev needs an AWS-owned reconciliation job that runs without a laptop and records health. EventBridge plus a dedicated Lambda keeps the scheduled job separate from public APIs and reuses the current normalized model. | T0037 adds a dev scheduled Lambda that reads Playground-only Roller config from AWS, imports bookingitems/tickets/payments/customers/products for the previous modified-date day by default, updates `jumpyard.booking_seed_runs`, and performs no Roller writes. | Revisit before production scheduling, timezone-specific schedules, larger backfill windows, or if Data API volume requires a Step Functions/SQS worker model. |
| `D0048` | 2026-05-22 | Check-in links should use server-owned opaque tokens, not booking numbers as link authority. | Booking references are visible to guests and staff and should not become the only credential for an SMS/deep-link check-in path. A high-entropy token lets JumpYard Cloud authorize link resume while keeping the normal booking lookup flow available. | T0038 creates dev-protected link creation and public token resolution. Raw tokens are response-only; Aurora stores only SHA-256 token hashes, channel, expiry, and opened/consumed timestamps. Resolving a token starts or resumes a JumpYard Cloud session without calling Roller. | Revisit when production SMS auth, signed universal links, token one-time-use policy, or staff/admin auth are implemented. |
| `D0049` | 2026-05-22 | SMS sending must be server-owned, dry-run-first, and audited without storing raw links. | SMS links carry check-in authority through opaque tokens and can generate provider cost. The server must own token creation, provider credentials, idempotency, and audit while keeping raw tokens and full link URLs out of logs and Aurora. | T0039 adds protected `POST /v1/check-in/session-links/send-sms`, defaults to dry-run, records `jumpyard.sms_deliveries`, and allows AWS SNS sends only when explicitly confirmed. The phone/admin UI does not send SMS directly. | Revisit before production SMS sender registration, staff/admin auth, consent rules, unsubscribe handling, or if another SMS provider replaces AWS SNS. |
| `D0050` | 2026-05-22 | SMS provider acceptance is not delivery; delivery status logs are required for real SMS validation. | T0041 showed AWS SNS accepted a message while the user did not receive it. T0042 delivery logs revealed the account is in SNS SMS sandbox mode and the provider rejected delivery to the unverified destination. | Dev SMS validation should check `jumpyard.sms_deliveries`, SNS delivery status logs, and SNS SMS sandbox status. A real user receipt test requires either verifying the destination phone in SNS sandbox or exiting SMS sandbox. | Revisit when switching SMS provider, leaving SNS sandbox, or adding production SMS monitoring. |
| `D0051` | 2026-05-22 | Phone SMS/deep links must resolve `jy_token` through JumpYard Cloud before rendering check-in state. | The phone app needs booking/session context to resume safely, but the raw token must stay an opaque server-owned credential and booking references must not become link authority. | T0044 makes the phone app detect `jy_token`, call `POST /v1/check-in/session-links/resolve`, and route from the returned JumpYard Cloud session state. Successful resolve responses may include a safe Aurora booking summary but not contact PII, raw Roller payloads, or secrets. | Revisit when public HTTPS app hosting, universal links, one-time token consumption, or production SMS auth are implemented. |
| `D0052` | 2026-05-22 | Booking-time SMS sending should start as a protected manual trigger before any automatic schedule. | Timing-based SMS can create cost, noise, and guest-facing links, and the AWS account is still in SNS sandbox mode. A planning-first trigger lets the team verify due booking selection, contact readiness, duplicate suppression, and link sending without enabling unattended guest messaging. | T0045 adds protected `POST /v1/check-in/session-links/send-due-sms`; it reads Aurora booking time windows, defaults to no-send planning, requires `confirmSend=true` for real sends, skips recent real sends, and defers EventBridge scheduling. | Revisit when public/mobile URL, consent/unsubscribe policy, SNS sandbox exit, staff/internal auth, and exact send timing are approved. |
| `D0053` | 2026-05-25 | Dev scheduled booking-time SMS should run internally through EventBridge but stay in planning mode until guest-facing SMS prerequisites are approved. | The operational flow should not require a staff/admin to manually trigger due SMS, but unattended real SMS with a `localhost` app URL and SNS sandbox limits would create poor guest messages and provider failures. | T0046 adds an internal EventBridge schedule that invokes the session Lambda every 5 minutes with `confirmSend=false`; the public HTTP endpoint remains token-protected, and real scheduled sends require an explicit config change in a later ticket. | Revisit when a public/mobile app URL, SNS sandbox exit or verified recipient policy, consent/unsubscribe policy, and production SMS sender setup are approved. |
| `D0054` | 2026-05-25 | Staff/admin handoff should use a server-issued staff token instead of a manually entered redeem dev code in the normal admin flow. | Staff redeem is a write-critical operation, but the T0027 temporary code was a development bridge, not an operator auth model. A server-issued short-lived token lets the admin app authenticate once, keeps passcodes server-side in Secrets Manager, and avoids exposing the lower-level redeem dev-token path to normal staff UI. | T0047 adds `POST /v1/staff/auth/login`, protects staff list/detail/redeem routes with the staff token, and removes the temporary dev-code input from the normal admin handoff panel. Production identity provider, roles, MFA, and token policy remain a follow-up. | Revisit before production rollout, multi-role staff permissions, or Cognito/SSO implementation. |
| `D0055` | 2026-05-25 | Confirmed scheduled booking-time SMS must require both explicit approval and a public HTTPS check-in URL. | A single boolean is too easy to flip accidentally for unattended SMS, and real sends with `localhost` links are unusable on guest phones. Scheduled sends also bypass the public dev-token route because they are internal EventBridge invocations, so the scheduler needs its own config and runtime guard. | T0049 keeps dev `confirmSend=false`, makes the scheduler base URL explicit, fails CDK config when `confirmSend=true` lacks the approval phrase or public HTTPS URL, and blocks EventBridge-shaped confirmed sends at runtime when those safeguards are absent. | Revisit when a public/mobile app URL, SNS sandbox exit or verified-recipient policy, sender identity, consent/unsubscribe copy, and timing policy are approved. |
| `D0056` | 2026-05-26 | T0040 is superseded by the T0050-T0053 payment sequence. | The old T0040 label was a blocked placeholder and caused confusing jumps backward after T0049. Pabel has now answered enough payment prerequisites to restart payment work as a staged sequence: readiness, new-booking payment execution, add-product payment execution, then new-booking basket order correction. | T0050 documents prerequisites and readiness without payment execution. T0051 owns new-booking payment package/drop-in execution. T0052 reuses the proven path for linked add-product drafts. T0057 is the integrated smoke checkpoint, and T0058 is reserved for stack production readiness. | Revisit if Roller says the PWA payment package cannot be used for this venue or if JumpYard chooses a hosted-payment fallback. |
| `D0057` | 2026-05-26 | Frontend payment calls are allowed only through Roller's approved payment package using the draft `paymentJwt`. | The normal production rule remains that the frontend must not call Roller directly or hold Roller credentials. Roller Payments via API is a special custom-checkout flow where the approved package uses a short-lived draft `paymentJwt` and public payment settings to create an Adyen session. | T0051 may render `@roller/ecom-payments` in the phone buy-entry flow and pass the response-only JWT in memory. The raw JWT must not be persisted, logged, printed, or rendered, and generic Roller REST calls remain server-side only. | Revisit if Roller changes the payment package contract, requires server-side payment replay, or if a hosted payment fallback is selected. |
| `D0058` | 2026-05-26 | Add-product payment uses the same narrow Roller package/JWT exception as new-booking payment. | Existing-booking add-products are separate linked draft bookings, so the payment path can reuse the approved T0051 custom-checkout model without exposing Roller credentials or mutating the original booking. | T0052 passes the add-product draft JWT only in memory to the payment component. Approved payment continues the original booking check-in flow, while the add-on draft remains a linked Roller booking owned by the add-product path. | Revisit when implementing add-on fulfillment reconciliation, payment-result webhooks, or a different same-booking update UX. |
| `D0059` | 2026-05-26 | New-booking payment happens after basket composition, not before add-ons. | A guest who buys entry should select the jump product, quantity, and add-ons before paying, so the flow creates one coherent Roller draft and one payment instead of charging before add-ons are known. | T0053 changes the phone buy-entry path to `time -> entry product/quantity -> add-ons -> contact -> review -> one draft -> one payment`. Existing-booking add-products remain a separate linked draft path from T0052. | Revisit if JumpYard chooses separate payments for add-ons or Roller requires add-ons to be sold outside the new-booking draft payload. |
| `D0060` | 2026-05-26 | Card entry must come from Roller's/Adyen's configured payment methods, not a JumpYard-side forced field. | The Roller payment package renders methods from the venue/payment-session configuration. The public T0054 smoke proved Swish can complete payment, while card/scheme is absent from the current rendered methods and public ecom config. Forcing a custom card form would bypass the approved Roller payment package and create PCI/security risk. | JumpYard should not hand-roll card collection. The next action is to ask Roller/Pabel to enable or confirm the `scheme` card method for Playground custom checkout so the documented Adyen Visa test card ending `1142` can be used. | Revisit only if Roller provides a different approved card-entry integration or confirms the current package can be configured client-side with an official flag. |
| `D0061` | 2026-05-26 | Paid new buy-entry bookings should continue directly into safety and QR handoff, not the existing-booking summary/add-ons loop. | A buy-entry guest has already selected entry, add-ons, contact details, and payment in the buy flow. Sending them back through the existing-booking summary creates a repeat add-ons/payment path and makes the flow feel like it restarted. | T0055 marks paid new-booking continuation as a buy-entry flow, starts/resumes the JumpYard Cloud check-in session, routes guest-in-progress sessions to safety, routes ready sessions to QR, and keeps existing-booking lookup behavior unchanged. | Revisit if JumpYard intentionally wants a post-payment review screen before safety or if payment redirect resume requires a separate persisted return state. |
| `D0062` | 2026-05-27 | Prepayment draft lifecycle follows authoritative Roller booking snapshots after payment. | `prepayment_booking_drafts` is JumpYard Cloud operational state, but Roller remains the source of truth for booking/payment settlement. A draft that remains `payment_pending` after Roller has created a paid booking creates confusing local state and weakens later reconciliation. | T0056 marks matching drafts `published` when lookup or webhook enrichment sees a settled Roller booking snapshot, sets local amount owing to zero, and writes a safe idempotent event-log row. Pending, unpaid, partial, and positive-amount-owing snapshots remain unchanged. | Revisit if Roller provides a dedicated payment webhook, draft lifecycle endpoint, or a more granular local payment state model is needed. |
| `D0063` | 2026-05-27 | Staging and live environments require a production-readiness gate before cloning the dev stack. | T0058 shows the dev stack is useful for Playground iteration, but it still has dev-only tokens/passcodes, public routes without API Gateway authorizers, SNS sandbox limits, incomplete observability/runbooks, and no production cutover/runbook. | Do not create staging/live by copying `infra/config/dev.json` until environment config, auth, observability, SMS, retention, webhook security, backfill/cutover, and rollback decisions are ticketed and approved. | Revisit after the T0058 blockers are implemented and a staging deployment review passes. |
| `D0064` | 2026-05-28 | Roller redemption payloads must include only structurally redeemable ticket ids. | Mixed baskets can contain entry tickets plus stock/add-on tickets, and Roller rejects non-redeemable product types with `Product type not accepted`. Display names are too fragile for this rule. | T0059 filters by structured product type metadata from tickets, booking items, and product cache, and re-applies the filter after the final Roller refresh before `POST /redemptions`. Stock/add-on/retail/gift-card/fee products can stay visible operationally but are excluded from Roller redemption. | Revisit if Roller provides a first-class redeemable flag or JumpYard changes product configuration so add-ons should be redeemed as tickets. |
| `D0065` | 2026-05-28 | Dev API hardening should start with explicit origins and CloudWatch-owned observability before staging/live auth work. | The team needs visibility into API volume, Roller outbound API calls, Lambda failures, and queue failures while still iterating in Playground. Tightening CORS and adding alarms/dashboard reduces risk without changing app flow semantics. | T0060 keeps route auth/WAF/rate limiting as future production-readiness work, but dev now has explicit CORS origins, API Gateway access logs, dashboard `jumpyard-check-in-dev-ops`, CloudWatch alarms, and safe Roller outbound API call counters. | Revisit when implementing production auth, WAF/rate limits, alarm notification routing, or staging/live configs. |
| `D0066` | 2026-05-28 | Dev API Gateway should use stage-level throttling before route authorizers or WAF are added. | The current guest and staff flows still need public HTTP entrypoints, while route authorizers and WAF/edge controls need a broader production boundary design. Stage throttling reduces burst/abuse risk now without changing app behavior. | T0061 configures dev `$default` API throttling at rate `25` requests/second and burst `50`, keeps detailed metrics enabled, and tracks/alarm on HTTP `429` throttled requests. Route-specific production limits, WAF/edge controls, and API Gateway authorizers remain future production-readiness work. | Revisit before staging/live API exposure, custom domain setup, or production auth/WAF implementation. |
| `D0067` | 2026-05-28 | Every JumpYard Cloud API route must have an explicit trust boundary before staging/live. | The dev API has mixed guest, staff, webhook, internal operations, and legacy dev routes behind one HTTP API. Applying one generic lock would either block guest check-in/payment or leave privileged operations too open. | T0062 classifies current routes into guest public, guest token, guest write, staff auth entry, staff protected, internal operations, Roller webhook, and legacy/dev-only classes. Later implementation should apply authorizers, WAF or equivalent edge controls, internal-only access, and route-specific limits according to that class. | Revisit when implementing production route auth/WAF controls or if the API route inventory changes materially. |
| `D0068` | 2026-05-28 | Guest email check-in links should use the same opaque token pattern as SMS. | Email and SMS are guest messaging channels for the same check-in session resume flow. Keeping both behind JumpYard Cloud token hashes avoids exposing booking numbers as authority and keeps raw links out of logs/Aurora. | T0063 adds an SES-ready email route, `jumpyard.email_deliveries`, dry-run/preview behavior, masked/hashed destinations, and a configured sender gate for confirmed sends. | Revisit when selecting the production email sender/domain, provider, consent text, branding, or unsubscribe policy. |
| `D0069` | 2026-05-28 | Guest SMS and email completion should come before broader staging/live readiness work. | SMS and email are core entry points for the visitor check-in journey. The team needs to prove both channels end-to-end before spending the next tickets on environment/cutover, route hardening, and other production-readiness work. | T0064 reorders the roadmap so T0065 completes SMS, T0066 completes email, T0067 proves a real dev SES email smoke, and unified booking-time guest messaging follows before broader production-readiness tickets. | Revisit if an external production deadline requires environment/cutover work before guest messaging is proven. |
| `D0070` | 2026-05-28 | Dev SMS success requires both JumpYard Cloud audit state and provider delivery-status confirmation. | AWS SNS can accept a publish request while downstream delivery still fails, especially while the account is sandboxed. A JumpYard `sms_sent` row proves the provider accepted the request from Lambda, but CloudWatch SNS delivery status proves the carrier/provider outcome. | T0065 keeps SMS responses audit-safe, stores delivery `jysms_mppg15lj_7c660ef2` in `jumpyard.sms_deliveries`, and checks SNS delivery status for `SUCCESS` during confirmed SMS smoke. | Revisit when leaving SNS sandbox, switching SMS providers, or adding production delivery dashboards/alarms. |
| `D0071` | 2026-05-28 | Dev email real sends must fail closed until AWS SES has a verified sender identity. | SES is currently sandboxed in `eu-north-1` and has no verified email/domain identities. Sending from an unverified sender would fail and could create misleading operational state. | T0066 keeps dry-run email planning available, records safe audit rows, returns sender/reply-to diagnostics, and blocks confirmed sends with `email_sender_not_configured` until a sender/domain is explicitly approved and verified. | Revisit when JumpYard chooses the production sender/domain, verifies SES identity, or exits SES sandbox. |
| `D0072` | 2026-05-28 | The first real dev email smoke may use `love@wrlds.com` as a verified SES email identity, but production should use a reviewed sender/domain. | The user approved `love@wrlds.com` for testing, and SES sandbox requires verified identities before real sends. This proves the server-owned email path without prematurely deciding production branding, DKIM domain setup, unsubscribe wording, or sandbox exit. | T0067 creates the tagged SES identity for `love@wrlds.com`, waits for verification, and only then may configure dev sender/reply-to and run a protected confirmed smoke. Production email remains a later readiness item. | Revisit when selecting a production sender domain/address or moving out of SES sandbox. |
| `D0073` | 2026-05-28 | Booking-time guest messaging should use one server-side scheduler/processor for SMS and email. | SMS and email are the same operational invitation: find bookings due before their jump time, check safety/eligibility, create opaque check-in links, audit delivery attempts, and avoid recent duplicate sends. Separate schedulers would make timing, idempotency, and failure behavior drift. | T0068 adds `POST /v1/check-in/session-links/send-due-messages`, keeps `send-due-sms` as SMS-only compatibility, and changes the dev EventBridge booking-time payload to invoke both channels in planning mode. | Revisit when enabling confirmed unattended sends, adding more channels, or replacing SNS/SES providers. |
| `D0074` | 2026-05-29 | Stabilization and full-flow proof come before broader staging/live production-readiness implementation. | The system now has many working pieces, but the team needs confidence that Data API sync, webhook enrichment, Aurora state, SMS/email messaging, phone check-in, staff handoff/redeem, and add-product/payment behavior work together before investing in cutover, WAF/auth, runbooks, retention, and live backfill. | T0069 reorders the near-term roadmap so T0070-T0074 are stabilization and verification tickets; environment/cutover and broader production-readiness tickets move after that gate. | Revisit if the team intentionally accepts production-readiness work before the full dev/Playground flow is proven together. |
| `D0075` | 2026-05-29 | Unattended guest SMS/email sends stay disabled until sender readiness gates pass. | T0072 confirms the current dev setup is ready for controlled smoke only: SNS remains sandboxed with one verified test phone, SES remains sandboxed with only `love@wrlds.com` verified, and production sender/domain/deliverability/monitoring choices are not complete. | The booking-time schedule must keep `confirmSend=false` until a later messaging ticket resolves SMS sandbox/sender policy, SES production sender/domain readiness, confirmed scheduled sends, and channel monitoring/runbooks. T0073 may run controlled smoke to approved destinations. | Revisit when SMS and email production readiness blockers are resolved and an explicit ticket enables confirmed unattended sends. |
| `D0076` | 2026-05-29 | SMS production unlock should be handled as an AWS Support/request workflow before any config change enables real guest sends. | T0073 proves the code path can send SMS, but the account is still in SMS sandbox and no End User Messaging sender ids or pools exist. Sending to arbitrary guest numbers requires provider/account approval, compliant transactional copy, expected volume, opt-in/consent explanation, and a sender-display decision. | T0074 documents the support-case package and keeps dev scheduled sends in planning mode. No support case, sender id registration, or confirmed unattended send is performed until the user approves the exact request and required business details. | Revisit after AWS approves production SMS access or if JumpYard chooses a different SMS provider. |
| `D0078` | 2026-06-01 | Existing-booking add-products should reuse the original booking contact server-side. | Asking the guest to re-enter contact details during add-product purchase is poor UX and creates duplicate/possibly conflicting contact data. Roller still requires customer details for the separate add-on draft, so JumpYard Cloud should resolve the original booking contact from Roller/Aurora instead of trusting the phone app to resubmit it. | T0079 phone add-product calls omit `customer`; JumpYard Cloud resolves first name, last name, email, and phone server-side and fails closed if the contact cannot be resolved. Postal-code behavior stays owned by Roller/Adyen. | Revisit if Roller supports creating add-product drafts without a customer object or if production data policy changes contact retention. |
| `D0079` | 2026-06-01 | Staff handoff may show limited guest identifiers in authenticated staff-only surfaces. | Staff need to identify the right guest quickly at handoff, and booking code alone is not enough in busy operations. Name/email search is useful for staff, but should not leak into public guest surfaces. | T0083-T0085 may add booking/customer name and email search to staff-authenticated handoff APIs/UI with masking or role limits where appropriate. Public phone UI should continue avoiding unnecessary PII display. | Revisit before production auth/retention policy, or if staff surfaces become public/shared-device without adequate access control. |
| `D0080` | 2026-06-01 | JumpYard-created booking contact can be reused from prepayment draft state for linked add-product drafts. | Roller booking detail exposes booking name and customer id, but not always email/phone. For bookings created through JumpYard Cloud, the original `prepayment_booking_drafts` row already contains the structured contact needed to create a later separate add-product draft. Reusing that server-owned state preserves the no-duplicate-contact UX without inventing data. | T0082 updates the booking Lambda to resolve email/phone from the original draft row by Roller unique id before falling back to Aurora `guest_profiles`. Raw payment JWTs are still response-only, and full contact values must not be printed in validation output. | Revisit when a production PII retention policy changes prepayment contact storage, or if Roller starts returning all required contact fields in booking detail. |
| `D0081` | 2026-06-02 | Staff/admin should use a separate Cloudflare Pages origin from the guest phone app. | Staff/admin is a staff-authenticated operating surface and should not be deployed as an accidental path inside the guest check-in Pages project. A separate origin keeps CORS, testing, and future access controls explicit. | T0087 targets `https://jumpyard-checkin-admin.pages.dev`, documents Cloudflare Pages settings, adds static admin headers, and prepares dev API CORS for that origin. Staff auth and secrets remain server-owned through JumpYard Cloud. | Revisit if WRLDS adopts a custom domain/router that hosts guest and staff apps behind a reviewed route/auth boundary. |
| `D0082` | 2026-06-02 | Webhook enrichment may use Roller guest detail for staff-only identity. | Staff-admin needs same-day guest names for bookings created or changed after the daily customer import. The webhook still starts with authoritative booking detail, but Roller booking detail can return only a customer/guest id. Official Roller docs confirm read-only `GET /guests/{guestId}`, where `guestId` is formerly/equivalent to `customerId`, so a single fallback read is safer than waiting for the next Data API customer window. | T0088 lets the webhook upsert `guest_profiles` with Roller customer id, first/last name in `latest_booking_context`, masked/hashed contact fields, and safe source metadata from booking detail or the documented guest-detail fallback. Responses/logs expose only booleans and status, not raw names/emails/phones. | Revisit before production retention policy, if Roller changes guest-detail behavior, or if Data API customer freshness makes the fallback unnecessary. |
| `D0083` | 2026-06-02 | Production guest messaging remains blocked until sender approvals and delivery gates are complete. | AWS read-only checks show both SMS and SES are still sandboxed. Dev can run controlled smokes, but production-like unattended sends require AWS sandbox/production access, approved sender identity/domain, final transactional copy, consent/support wording, volume/peak-rate inputs, monitoring, and environment separation. | T0089 documents the exact unlock gates in `GUEST_MESSAGING_PRODUCTION_UNLOCK.md` and keeps EventBridge due-message processing in planning mode with `confirmSend=false`. No support cases, sender identities, domains, resources, or unattended sends are enabled in T0089. | Revisit when JumpYard/WRLDS provides final domain/sender/copy/volume inputs and explicitly approves AWS support requests or production sender setup. |
| `D0084` | 2026-06-02 | Gift cards and multi-visit passes must be proven before broader production readiness. | Core card checkout works, but JumpYard still needs to know whether a guest can pay a buy-entry booking with gift card value and whether multi-visit passes act as payment, entitlement, ticket, membership, or another Roller object. Guessing here could break payment totals or redeem eligibility. | T0090 must discover the exact Roller Playground behavior first. T0091 may implement only confirmed gift-card and multi-visit paths. T0092 must smoke test those cases through phone flow, Aurora, Roller booking state, check-in session, and staff redeem/eligibility before the roadmap returns to monitoring/cutover readiness. | Revisit after T0090 documents Roller responses and after any clarification from Josh/Joao/Pabel changes the supported flow. |
| `D0085` | 2026-06-02 | Gift card can proceed with active Playground test data; multi-visit remains blocked pending Roller confirmation. | T0090 showed `POST /bookings/draft/costs` accepts `giftCards` and returns `giftCardErrors`. Two paid Venue Manager gift-card fixtures appeared in `/data/giftcards` and applied successfully in booking costs: `100 kr` reduced a `200 kr` quote to `amountOwing=100`, and `500 kr` reduced it to `amountOwing=0`. Gift cards are separate from discounts in the documented booking payload and are stored-value tenders in Roller Help Center. T0090 also found membership products for 10-/20-/30-card sales, confirmed documented `GET /customers/{customerId}/multi-passes` is reachable, and tested paid `10-Kort` booking `5101046`. That paid `10-Kort` customer returned zero multi-pass balances and did not auto-apply in booking costs; `multiPassAllocations` remained empty. | T0091 should implement gift-card checkout using the proven full/partial Playground gift-card fixtures. T0091 must not implement multi-visit use unless Roller confirms API auto-apply behavior or provides a real beta multi-pass fixture that proves it. T0092 should smoke normal card, invalid gift-card, partial gift-card, full gift-card, and later multi-visit when supported. | Revisit when Josh/Joao/Pabel confirms the multi-visit API model. |
| `D0086` | 2026-06-02 | V1 gift-card payment should use Draft Booking, while multi-visit code support stays validation-only. | Pabel/project notes distinguish gift-card use as payment in Booking Costs/Create Draft Booking from gift-card administration, which is not supported by public API for this pilot. Payment Link is a poor fit for gift cards/add-ons because it does not keep the guest in the PWA and may not support the same gift-card/discount behavior. Nacka multi-visit appears closer to a membership/discount-code validation model than the beta multi-pass balance endpoint, and Roller does not provide a clean public remaining-balance field for this setup. | T0091 implements gift-card use in the existing Draft Booking checkout path only. Multi-visit V1 must not show remaining visits. A later validation ticket should test whether membership/10-card codes can be sent as `discounts: [{ code }]` or another Roller-supported field, letting Roller validate and reject when uses are exhausted. Existing-booking add-ons should keep using separate linked drafts, not Payment Link. | Revisit if Roller provides a confirmed multi-visit code payload, a beta multi-pass fixture with balances, or public API support for gift-card administration. |
| `D0087` | 2026-06-02 | Full gift-card coverage should publish the Roller draft without rendering card payment. | T0090 proved active gift cards can reduce `amountOwing` to `0`, and Roller documents `POST /bookings/draft/publish` as the no-payment publish path for drafts that owe `0.0`. Showing Adyen/card entry when no money is owed would confuse guests and risk a stuck checkout. | T0091 uses the existing server-side Draft Booking path, forwards `giftCards`, returns only safe gift-card metadata, and calls `POST /bookings/draft/publish` only when `amountOwing=0` and a gift card was supplied. If publish fails, JumpYard Cloud fails closed instead of pretending the booking is paid. | Revisit after T0092 proves the public full-gift-card flow through Aurora/webhook/session/staff redeem. |
| `D0088` | 2026-06-03 | Current Nacka `10-Kort` should be treated as membership/discount-code validation, not beta multi-pass balance. | Gustav clarified that the current `10-Kort` setup is not Roller beta multi-pass. T0097 rechecked paid `10-Kort` fixture booking `5101046`: `GET /customers/4045520/multi-passes` still returned zero balances, while the masked `10-Kort` ticket/code sent as `discounts: [{ code }]` reduced Booking Costs by 100%. Official Roller docs also direct discount validation toward Booking Costs, not the deprecated validate-discount endpoint. | V1 should let guests enter a code, send it to Roller through the `discounts` payload, and treat it as accepted only when Roller reduces `amountOwing` or returns positive discount value. V1 must not display remaining visits or implement local visit counting for this setup. | Revisit when Roller provides a beta multi-pass fixture, a confirmed balance endpoint for the current Nacka setup, or proof that draft/publish consumes and blocks exhausted membership codes differently than T0098 observed. |
| `D0089` | 2026-06-03 | JumpYard must not show remaining-use counts for the current Nacka `10-Kort` V1. | T0098 created and published one Playground booking `5101114` using the masked `10-Kort` code. After the write, `GET /customers/{customerId}/multi-passes` still returned zero balances for both the original pass customer and the smoke-booking customer, Booking Costs still accepted the same code for up to ten entries, and `/data/bookingitems` showed discount evidence but no remaining balance. | The guest UI may show code accepted/rejected and amount reduced, but must not show "10 kvar", "9 kvar", or a locally calculated use counter. JumpYard Cloud should not own pass-balance math for this setup. | Revisit only if Roller exposes a confirmed remaining-use source for this venue setup or moves JumpYard Nacka to the beta multi-pass balance model. |
| `D0090` | 2026-06-03 | Guest-facing code entry should be named `Klippkort` and sent as a Roller discount code. | Gustav clarified the same flow may cover 10-, 20-, and 30-card products, so naming the field `10-Kort` would be too narrow. T0098 showed the code covers eligible entry/session pass products but not add-ons, and Roller exposes applied discount rather than remaining balance. | T0099 uses a `Klippkort` field in the phone buy-entry checkout, forwards the value through JumpYard Cloud as `discounts: [{ code }]`, blocks no-effect codes, and shows only the applied amount. | Revisit if JumpYard changes product naming or Roller provides a true multi-pass balance model for this venue. |
| `D0091` | 2026-06-04 | T0101 adds an operations runbook before adding more dev observability resources. | Read-only AWS checks confirmed the existing dev dashboard, API/Lambda/Roller/DLQ/throttle alarms, and Lambda log groups are present and healthy. The highest immediate gap is not another dev alarm, but clear operator steps for what each signal means, where to look, what is safe to do first, and who owns escalation. | Dev keeps the existing CDK observability resources unchanged for T0101 and adds `OPERATIONS_RUNBOOK.md` as the source of truth for Data API, webhook, booking/payment, gift-card/Klippkort, messaging, and staff redeem incident response. | Revisit when wiring alarm notifications, channel-specific SMS/email alarms, Aurora/scheduler health metrics, or staging/live thresholds. |
| `D0092` | 2026-06-09 | Phone add-on display prices should come from the existing JumpYard Cloud availability contract, not a separate frontend catalog route. | The frontend must not call Roller, and the booking Lambda already owns Roller availability plus the cached Roller product catalog. Extending `POST /v1/bookings/availability` keeps buy-entry and existing-booking add-ons on one server-owned source without adding a new public route. | T0113 removes static frontend add-on prices, returns stock add-on rows with product ids/prices from `product_catalog_cache`, keeps SkyRider capacity-gated through Roller availability, and leaves quote/draft/payment totals server-owned. | Revisit if Gustav approves a broader guest-facing add-on catalog or if stock add-on pricing needs a dedicated server-side catalog endpoint for non-booking contexts. |
| `D0093` | 2026-06-09 | `REPO_CURRENT_STATE.md` ticket state must be self-validated by `npm run validate`. | The file has both a snapshot and ticket tables, so stale duplicate state can mislead follow-up work if only one section is updated. | `scripts/validate-template.js` checks that snapshot completed tickets match the Completed Tickets table, the snapshot current ticket matches the Current Ticket table, completed tickets are not listed as current or confirmed next, and recommended next ticket ids still appear in current or confirmed-next state. | Revisit if `REPO_CURRENT_STATE.md` is simplified to a single source of ticket state. |
| `D0094` | 2026-06-15 | Active ticket context must be validated across `CODEX_TASK.md` and `REPO_CURRENT_STATE.md`, and old working history should be archived before deletion. | After T0125, `REPO_CURRENT_STATE.md` correctly said no active ticket while `CODEX_TASK.md` still described T0125, which could mislead the next agent. The repository also has long technical and chat-derived history that should be preserved while keeping current context short. | T0127 adds dedicated current-ticket validation to `npm run validate`, documents an audit-first context hygiene workflow, and treats future history moves as archive-before-delete migrations into `docs/history/` and `docs/roadmap/` after validators support the new locations. | Revisit when historical sections are actually migrated or if `CODEX_TASK.md` is replaced by a different ticket handoff mechanism. |
| `D0095` | 2026-06-15 | Source-of-truth workflow documentation is written in English by default, while exact Swedish is preserved only for app-facing or evidence-specific cases. | Planning can happen in Swedish, but repository workflow docs, skills, audit reports, ticket summaries, decisions, and validators need a consistent default language. Swedish must still remain exact for user-facing UX copy, staff/admin UI labels, app/business operational terms, quoted evidence, and intentionally copied raw history. | Context-hygiene work summarizes Swedish chat/history into English unless exact Swedish wording matters, does not normalize Swedish UI strings such as `Betalning`, `Presentkort`, `Klippkort`, `Lämna ut vid incheckning`, `Hämtas efter hoppet`, and `Övrigt i bokningen`, and validators do not enforce a general language choice. | Revisit only if the project adopts a different source-of-truth documentation language policy. |
| `D0096` | 2026-06-15 | Active source-of-truth files should stay short and link to archive-backed history. | T0128 moved completed tickets, validation evidence, old project narrative, done followups, and broad roadmap planning out of active startup files so future agents can read the current state quickly without losing historical evidence. | `PROJECT_CONTEXT.md`, `REPO_CURRENT_STATE.md`, `FOLLOWUPS.md`, and `TEST_PLAN.md` stay focused on current facts and link to `docs/history/` and `docs/roadmap/`; validators protect archive presence, links, counts, and duplicate-id hygiene. | Revisit if the project changes its ticket/source-of-truth workflow or needs a different archive index. |
| `D0097` | 2026-06-17 | Park-test rollout should proceed through a gated ticket sequence before any broader visitor rollout. | The park-test plan moves from dev/Playground toward Roller Live / JumpYard Nacka and includes higher-risk AWS, Live API, webhook, booking lookup, catalog/index readiness, add-on, payment, settlement reconciliation, redeem, receipt, frontend rehearsal, and UX readiness steps that need separate approvals and rollback points. | T0144 records the park-test sequence as one-ticket-at-a-time work: audit first, then environment contract/config guards, synth/deploy preflight, explicitly approved AWS/Live steps, controlled quote/draft/payment/existing-booking lookup smokes, catalog/index readiness, add-on/payment/reconciliation/redeem smokes, receipt/confirmation readiness, post-receipt readiness gaps, handout/payment readiness, frontend redeem rehearsal, guest contact lookup, UI/UX readiness, assisted visitor test, and outcome/go-no-go documentation. The phone/admin source code remains shared, with environment-specific API targets rather than frontend Roller calls. | Revisit after T0180 go/no-go or if WRLDS changes the park-test architecture or approval model. |
| `D0098` | 2026-06-17 | Park-test is a separate WRLDS environment in the existing dev AWS account and region, with separate resources and Roller Live access only through JumpYard Cloud. | The park test needs to exercise Roller Live / JumpYard Nacka without disturbing the current dev/Playground stack or exposing Roller from the frontend. Using the same AWS account/region keeps operational ownership simple for the limited test, while a separate resource namespace and database prevent dev data/config bleed. | `park-test` uses account `376129878018`, region `eu-north-1`, planned resource prefix `jumpyard-check-in-park-test`, its own Aurora/database, secrets, SSM parameters, API, queues, schedules, logs, alarms, and frontend deployment config. Dev remains `jumpyard-check-in-dev` and Roller Playground. Park-test data is tagged `WRLDS:DataClassification=confidential`; resources are not created until later explicitly approved tickets. | Revisit after T0180 go/no-go, if WRLDS chooses a separate AWS account/region, or if data classification/cost-center policy changes before T0150 deploy approval. |
| `D0099` | 2026-06-17 | CDK config validation should allow `park-test` only when the config matches the T0146 contract, while dev remains Playground-only. | The project needs a code-level guard before any synthable park-test config exists, so accidental dev-to-Live wiring or incomplete park-test config fails before synth/deploy. | `infra/lib/config.ts` reads `WRLDS:Environment` as `dev` or `park-test`: dev must use Roller Playground and `https://api.play.roller.app`; park-test must use account `376129878018`, region `eu-north-1`, resource prefix `jumpyard-check-in-park-test`, Roller Live `https://api.roller.app`, `WRLDS:DataClassification=confidential`, and `bookingTimeSms.confirmSend=false`. T0147 adds local config-guard validation without creating park-test config or resources. | Revisit in T0148/T0152 if the park-test config shape or live-write gate model changes. |
| `D0100` | 2026-06-17 | Long environment prefixes use a compact raw-payload S3 bucket suffix while preserving existing shorter environment bucket names. | T0148 found that `jumpyard-check-in-park-test-raw-payloads-376129878018-eu-north-1` exceeds S3's 63-character bucket-name limit. Changing the standard pattern for all environments would risk a dev bucket replacement, so only over-limit names use the compact suffix. | Dev keeps the existing `${resourcePrefix}-raw-payloads-${account}-${region}` pattern. Park-test synthesizes `jumpyard-check-in-park-test-raw-376129878018-eu-north-1`, preserving the park-test prefix and separate ownership while staying within S3 naming limits. | Revisit if resource prefixes become longer than the compact pattern can support or if bucket naming moves to an explicit per-environment config field. |
| `D0101` | 2026-06-18 | The first park-test deploy requires the T0149 preflight, explicit T0150 approval, and sequential template diffs before resource creation. | The first park-test stack creates many separate AWS resources and must leave dev untouched. T0149 also found that default CDK change-set diff for a never-deployed stack can leave an empty CloudFormation `REVIEW_IN_PROGRESS` stack shell, so the safest pre-deploy review starts with template diff. | T0150 must verify AWS identity/account/region/tags, confirm no park-test stack exists, run local validations, run dev and park-test CDK commands sequentially, require dev template diff to be clean, review park-test additive resources, and stop if rollback ownership or Live gates are ambiguous. | Revisit after T0150 deploy, if deployment automation replaces local CDK commands, or if WRLDS changes the park-test approval model. |
| `D0102` | 2026-06-18 | Park-test must not configure account-wide SNS SMS delivery-status attributes until park-test guest messaging is explicitly scoped. | SNS SMS attributes are account-wide, not environment-scoped. A second park-test custom resource would have pointed account-level delivery diagnostics at a park-test role and could have affected dev SMS diagnostics. | T0150 keeps the SNS SMS delivery-status custom resource only for `WRLDS:Environment=dev`. Park-test does not create `jumpyard-check-in-park-test-sns-sms-delivery-status`; account SMS attributes still point to `jumpyard-check-in-dev-sns-sms-delivery-status`. | Revisit in the park-test messaging/secrets ticket if park-test SMS sending is explicitly approved. |
| `D0103` | 2026-06-22 | Park-test sensitive runtime gates start closed and are opened only by scoped tickets. | Park-test points at Roller Live config but must not accidentally issue staff tokens, process Live webhooks, create drafts/payment starts, redeem tickets, or send guest messages before each path has a controlled test ticket. | `infra/config/park-test.json` defaults emergency stop on and staff auth, guest message sends, webhook processing, booking draft writes, and redeem writes off. CDK maps these booleans into Lambda environment variables and the handlers fail closed on the sensitive paths. Dev keeps existing Playground behavior enabled. | Revisit when a scoped park-test ticket explicitly enables the next controlled Live read/write path. |
| `D0104` | 2026-06-22 | Roller Live read-only preflight tooling must use a hard endpoint allowlist and stop before non-scoped data reads or any write. | T0153 is the first scoped Roller Live step and must not become an accidental quote, draft, booking lookup, webhook, redeem, payment, customer, or ticket read. | `infra/scripts/roller-live-readonly-preflight.ts` allows only `GET /venues/me`, `GET /products`, and `GET /product-availability` after auth. It blocks write-like and sensitive endpoints. T0153 confirmed Live access through the park-test secret and verified Nacka venue/product/availability/payment-settings facts without writes. | Revisit if Roller requires a different documented read-only auth path or when a later ticket scopes another endpoint. |
| `D0105` | 2026-06-22 | Roller Live webhook registration must be dry-run planned before any external registration. | Live webhook registration is an external write that can affect real Nacka booking signals. The team needs the exact endpoint, auth header, events, duplicate behavior, and rollback template before T0155 can create or match a webhook. | T0154 adds a dry-run-only plan command that reads AWS metadata, prints `POST https://api.roller.app/webhooks`, endpoint `POST /v1/roller/webhooks/bookings`, header `x-roller-apikey`, events `Created`, `Updated`, `Cancelled`, `tickets=true`, duplicate rules, and rollback template. It rejects apply/delete/register args and makes no Roller requests. | Revisit in T0155 when explicit approval allows registration or if Roller confirms a different Live webhook auth/removal mechanism. |
| `D0106` | 2026-06-23 | Roller Live webhook registration can be active while park-test processing stays closed. | Registering the external Live webhook proves delivery configuration without letting Live events mutate park-test Aurora or trigger check-in behavior before the next scoped gate. | T0155 registered/matched Roller Live webhook `1465` for the park-test endpoint and kept `ENABLE_ROLLER_WEBHOOK_PROCESSING=false` with emergency stop on. Safe intake smoke returned `ignored_disabled` and inserted no Aurora webhook row. | Revisit when a future ticket explicitly enables park-test webhook processing or changes production webhook auth/removal policy. |
| `D0107` | 2026-06-23 | Park-test frontend uses separate Cloudflare Pages targets with the same source code, not source forks. | The team needs dev/Playground to remain available while park-test points at the separate JumpYard Cloud API. Duplicating the phone/admin apps would create drift; a deployment-time API env var is enough. | Phone/admin source defaults still point to dev, park-test builds set `NEXT_PUBLIC_JUMPYARD_CLOUD_API_BASE_URL=https://ij4rnaui2b.execute-api.eu-north-1.amazonaws.com`, and the park-test API CORS allows only the reviewed park-test Pages origins. Frontend still calls JumpYard Cloud only. | Revisit if WRLDS chooses custom domains, renames the Cloudflare Pages projects, or changes the frontend deployment platform. |
| `D0108` | 2026-06-23 | The first Roller Live quote smoke should use a local guarded costs-only tool. | A costs-only smoke proves Live availability/product selection and `/bookings/draft/costs` before exposing quote traffic through the public park-test API/Lambda path. | T0157 allows only Roller auth, `GET /product-availability`, and `POST /bookings/draft/costs`; it creates no draft, payment, redeem, webhook processing, Aurora rows, frontend traffic, SMS, or email. | Revisit if a later ticket requires a deployed park-test API quote smoke before draft/payment testing. |
| `D0109` | 2026-06-23 | Park-test must prove existing-booking lookup and existing-booking add-ons before controlled redeem or visitor testing. | The real visitor scenario starts with a guest-entered booking reference, then may add products before check-in. Treating lookup/add-ons as implicit inside redeem or visitor testing would hide important Roller Live, Aurora, PII, and rollback behavior. | The forward park-test backlog keeps T0160 as one controlled Live booking lookup, T0161 for catalog/index readiness, T0162-T0164 for existing-booking add-on contact/payment proof, T0165 for linked add-on settlement reconciliation, T0166 for controlled redeem, T0167 for receipt/confirmation handling, T0168-T0173 for readiness gaps found after the receipt proof, T0174/T0175 for handout and payment-method readiness, T0176 for frontend redeem rehearsal, T0177 for guest contact lookup, T0178 for UI/UX readiness, T0179 for assisted visitor testing, and T0180 for go/no-go. | Revisit if JumpYard removes existing-booking add-ons from the park-test scenario or changes the add-on architecture decision. |
| `D0110` | 2026-06-23 | The first Roller Live draft smoke should create the external draft through local guarded tooling while keeping public park-test API draft writes closed. | Creating the first Live draft proves Roller draft creation and payment JWT presence without briefly opening an unauthenticated public API route or creating Aurora state before the controlled payment/lookup tickets. | T0158 requires `--apply` plus `ROLLER_LIVE_DRAFT_SMOKE_ALLOW_WRITE`, allows only auth, availability, costs, and draft endpoints, creates one Roller Live draft, prints only safe references/JWT presence, and keeps AWS runtime gates plus Aurora writes closed. | Revisit in T0159/T0160 when testing the actual park-test API/payment/lookup path. |
| `D0111` | 2026-06-24 | The first park-test Live payment opens only a temporary booking draft/payment-start gate, while Live lookup remains a separate gate. | The internal payment smoke needed the phone PWA to create a paid Roller Live booking, but broad Live lookup/sync, redeem, webhook processing, SMS, email, and visitor rollout must stay isolated. | T0159 uses a separate reviewed payment-smoke CDK config and approval phrase, then deploys the normal closed config after the smoke. The paid booking becomes the controlled target for T0160. | Revisit when T0160 opens and validates the minimum Live lookup path. |
| `D0112` | 2026-06-24 | The first park-test Live lookup opens only temporary exact-identifier access. | Existing-booking lookup is required before add-ons/redeem/visitor testing, but broad Live booking lookup or all-day export would expose more guest data than needed for the first smoke. | T0160 uses a separate reviewed lookup-smoke CDK config, approval phrase, and exact allowlist for booking `166447399` plus unique id `68b3bbb4-9a46-4379-96ac-bc7157f2fb3e`. Normal park-test config keeps Live lookup disabled, and public Live lookup output omits raw guest names/contact data. | Revisit when T0161 decides lookup/index readiness, when T0162/T0163 need controlled add-on or redeem lookup behavior, or before enabling normal visitor traffic. |
| `D0113` | 2026-06-24 | Park-test should verify Live product/add-on catalog and booking-index strategy before the next Live add-on or redeem smoke. | The first real park-test scenario must be able to find a guest-entered booking code and show the right Nacka Live products/add-ons. Moving directly from one allowlisted lookup to add-on/redeem risks relying on stale Playground product ids or an unreviewed all-day booking data strategy. | T0161 selected REST-on-demand lookup and confirmed Live product/add-on IDs. Existing add-on contact/payment, linked settlement reconciliation, redeem, receipt/confirmation, add-on visibility, post-payment sync, gate naming, lookup mode, webhook readiness, handout/payment readiness, frontend redeem rehearsal, UI/UX readiness, visitor, and go/no-go tickets continue as scoped tickets. | Revisit if the park-test scenario no longer includes add-ons or if broad same-day booking indexing is explicitly approved. |
| `D0114` | 2026-06-24 | The first assisted park test should use REST-on-demand booking lookup, not a broad same-day booking import. | T0160 proved exact Live booking lookup, and T0161 confirmed required Live products/add-ons without reading bookings. For the first assisted park test, asking the guest to enter a booking code and caching only that normalized lookup keeps data exposure smaller than importing all of today's guests. | T0162/T0163 should continue from guest-entered booking lookup and store only explicitly looked-up normalized snapshots in Aurora. Same-day booking indexing, Data API export, webhook processing, staff queue dashboards, and staff identity search remain later scoped gates. | Revisit before unassisted visitor rollout, broader park operations, staff queue/search needs, or if REST lookup latency/reliability is not acceptable. |
| `D0119` | 2026-06-25 | Linked add-on settlement updates both the prepayment draft and booking link after a paid Roller snapshot. | Staff handout/redeem state needs both local records to agree. A paid add-on booking with only `prepayment_booking_drafts.status='published'` or only a linked Roller id is ambiguous for operations. | The lookup/webhook reconciliation path marks the matching add-product prepayment draft `published`, sets amount owing to `0`, writes `booking_links.linked_booking_reference`, marks the link `published`, and records settlement events only after Roller reports the linked add-on booking as settled. | Revisit if Roller adds a dedicated payment-settlement webhook or if same-booking add-ons replace separate linked drafts. |
| `D0120` | 2026-06-25 | Controlled Live redeem may temporarily bypass emergency stop only with exact booking and ticket allowlists. | T0166 needed to prove the real staff-confirmed Roller Live redemption path while keeping park-test closed for normal visitor traffic. A broad staff-auth/redeem unlock would risk consuming unintended Live tickets. | The reviewed redeem-smoke config requires the approval phrase, staff auth enabled, redeem writes enabled, and exact allowlist entries for both booking/unique id and every selected ticket id. The redeem Lambda can use Roller Live only in `park-test` with T0166 enabled, `https://api.roller.app`, and a non-empty allowlist; normal `park-test.json` redeploy closes staff auth and redeem again. | Revisit before T0179 visitor testing, before broad redeem enablement, or if Roller exposes a safer redemption sandbox/rollback model. |
| `D0121` | 2026-06-25 | Park-test PWA receipts should use Roller-native confirmations. | Roller owns the booking and payment of record, while JumpYard guest SMS/email sending remains closed for park-test. Sending a separate JumpYard receipt now would duplicate the source-of-truth receipt path and create extra sender/readiness scope. | T0167 sets `sendConfirmations=true` for phone PWA new-booking drafts and existing-booking add-on drafts. The booking Lambda already forwards that flag to Roller and logs the safe boolean in draft success events. Guest copy says Roller sends the confirmation/receipt to the booking email. The 2026-06-26 controlled new-booking proof received the Roller booking confirmation email; existing-booking add-on receipt delivery after T0167 remains unproven. | Revisit if Roller confirmations do not arrive for a scoped add-on proof, if JumpYard needs branded receipts, or when production guest messaging is explicitly scoped. |
| `D0122` | 2026-06-26 | Park-test readiness tickets move before frontend redeem rehearsal. | The 2026-06-26 receipt proof passed email delivery but exposed missing new-booking add-ons, post-payment sync failure, and confusing technical gate names. Those are park-test readiness issues that should be resolved before another front-of-house rehearsal. | T0168-T0173 cover add-on visibility, post-payment sync, gate naming/runbook, broader assisted lookup mode, assisted email lookup, and webhook/reconciliation readiness. T0174/T0175 then handle handout UI and payment-method readiness before the T0176 frontend redeem rehearsal. | Revisit after T0176 or if park-test scope changes. |
| `D0123` | 2026-06-26 | Park-test Live phone add-on catalog mapping is separate from existing-booking add-on write gates. | The T0167 receipt proof showed no new-booking add-ons because the known Nacka Live add-on ids/prices were only available while the T0162 existing-booking add-on smoke gate was open. Read-only product mapping is not the same risk as creating add-on drafts or taking payments. | BookingHandler keeps the T0162 write/allowlist gate unchanged, but `loadPhoneAddonProducts("live")` can use the known Nacka phone add-ons: SkyRider parent `970335` with child `970336`, socks `970338`, lock `970334`, and coffee `970352`. This lets availability return priced add-ons without opening draft writes, lookup, redeem, webhooks, SMS, or email. | Revisit if JumpYard changes the guest-facing add-on catalog, if Roller Live product ids change, or when product cache import becomes the preferred park-test catalog source. |
| `D0124` | 2026-06-29 | Park-test gate names are documented aliases, not runtime renames. | T0170 makes the safety gates understandable for planning and test-day operation without changing already deployed CDK/Lambda variable names in the same ticket. Renaming runtime switches is a migration because it touches config, code, validators, deployment, readback, and rollback. | Use [docs/t0170-park-test-gate-runbook.md](docs/t0170-park-test-gate-runbook.md) as the translation table from human gate names to current config keys and Lambda env vars. | Revisit only in a dedicated gate-name migration ticket. |
| `D0125` | 2026-06-29 | Post-payment new-booking sync is draft-backed, not broad lookup. | Park-test may refresh a paid new booking from Roller Live after checkout only when the lookup identifier matches a recent local `new_booking` prepayment draft created by JumpYard Cloud. | This fixes the post-payment recovery path without opening general Live booking lookup. | Revisit when T0171 defines the assisted visitor lookup mode or if Roller adds a dedicated payment-settlement webhook/status API. |
| `D0126` | 2026-06-29 | Ticket-type handout and payment-method readiness must be explicit before assisted visitor testing. | The T0169 phone proof reached safety/done, but showed two operational gaps: staff need to see entry duration/ticket type to hand out the right band/color, and card/Apple Pay/Swish behavior must be understood before a real visitor is put through the flow. | The forward roadmap now has `T0174` for ready-for-entry ticket-type handout UI and `T0175` for payment-method readiness before the T0176 frontend redeem rehearsal, final UI/UX readiness, and the assisted visitor test. | Revisit after T0175 decides the approved payment method set for the park test. |
| `D0127` | 2026-06-29 | Assisted park-test existing-booking lookup is single-code, date-scoped REST lookup, not a same-day booking import. | The park needs to pick an unknown visitor without knowing their booking id beforehand, but importing all same-day bookings would expose more Live guest data than the first assisted test needs. | T0171 adds a separate assisted lookup gate/config. It accepts only booking-reference-like ids or Roller UUIDs, reads exactly `GET /bookings/{identifier}`, validates the returned booking date against the approved operating dates, uses the Nacka-scoped Roller key plus venue id `50871` when available, and stores only the normalized looked-up snapshot in Aurora. Existing booking rows/add-ons/tickets are preserved; payment, add-on writes, redeem, webhooks, staff auth, SMS/email, and visitor traffic stay separate gates. | Revisit before broader visitor rollout, staff search/queue dashboards, all-day booking indexing, or if Roller booking detail cannot provide enough scope evidence for the selected park-test date. |
| `D0128` | 2026-06-29 | Public assisted email lookup needs a confirmed Roller API contract before implementation. | Venue Manager can search bookings by email, but current documented API evidence supports specific booking detail by id/reference and guest detail by guest id. Data API is not real-time and does not support specific-record lookup, so it would push T0172 toward broad guest-data import. | Do not add public `email -> booking` lookup to the PWA in T0172. If a visitor lacks a booking code, staff should search Roller Venue Manager by email and enter the booking code into the T0171 lookup. | Revisited on 2026-06-30; superseded by D0136/T0177 after current Roller REST search docs and read-only Live smoke evidence confirmed the endpoint shape. |
| `D0129` | 2026-06-29 | First assisted park-test keeps Live webhook processing off and confirms outcomes on demand. | Webhook `1465` is registered and auth/intake reachability is proven, but enabling processing would let external Roller Live booking events mutate park-test Aurora and is not needed to confirm the supervised flow. Current webhook registration is booking-event based, not a dedicated payment or redeem success source. | Keep `ENABLE_ROLLER_WEBHOOK_PROCESSING=false`. Use draft-backed post-payment sync for new bookings, T0171 REST lookup for existing bookings, scoped REST settlement for linked add-ons, and synchronous Roller `POST /redemptions` success plus Aurora audit for redeem confirmation. Use staff/manual Roller fallback for uncertain redeem outcomes. | Revisit before unassisted traffic, background staff queues, automatic add-on settlement, production webhook processing, or if Roller confirms payment/redeem-specific webhook contracts. |
| `D0130` | 2026-06-29 | Park-test readiness ticket ids put handout and payment before frontend redeem rehearsal. | The final redeem rehearsal is more useful after the done/handout screen shows QR and ticket duration, and after the team knows whether the park-test payment posture is card-only or needs Apple Pay/Swish configuration work. | `T0174` is Ready-for-entry handout UI, `T0175` is Payment method readiness, and `T0176` is Frontend redeem rehearsal. | Revisit if payment-method investigation or handout UI work is removed from the park-test scope. |
| `D0131` | 2026-06-29 | Apple Pay should be actively unblocked before the park test, with card as fallback. | T0159 and T0169 proved card payment in Roller Live, while Apple Pay collapsed on iPhone. T0175 found the park-test Pages domain returned `404` for the Apple Pay domain-association file, which is a concrete JumpYard-side blocker for Apple Pay web validation. | Add the Adyen Apple Pay domain-association file to the phone app, deploy it to the park-test Pages domain, ask Roller/Adyen to register/approve that domain for the Live merchant configuration, and run one iPhone smoke. If Apple Pay still fails before Wednesday, use card as the assisted-test fallback. | Revisit after the deployed association file is publicly reachable and Roller/Adyen has confirmed domain registration or a specific merchant-validation error. |
| `D0132` | 2026-06-29 | Park-test new-booking validation locks on approved Nacka entry/family parent products plus Live availability, not static child product ids. | Roller Live can return different child variation ids for different times/prices. A static child allowlist blocked valid products such as `1189809` even though Roller availability returned them for the selected slot. | Park-test payment-sync mode accepts only child variations returned by Roller Live availability under the approved Nacka 60/90/120 entry and family parent products. This does not open arbitrary products, existing-booking add-ons, redeem, webhooks, staff auth, SMS, or JumpYard-owned email. | Revisit when replacing static park-test mappings with a managed Live product catalog/import. |
| `D0133` | 2026-06-29 | Frontend redeem rehearsal uses a separate staff-auth-only gate, not the T0166 redeem-write gate. | Reusing T0166 would reopen the real Roller Live redeem write path, while T0176 only needs Love to inspect/rehearse the deployed staff UI before a visitor test. | Add `infra/config/park-test-frontend-redeem-rehearsal.json`, `T0176_FRONTEND_REDEEM_REHEARSAL_APPROVED`, and `ENABLE_T0176_FRONTEND_REDEEM_REHEARSAL`. The T0176 gate can enable staff auth under emergency stop for exact allowlisted check-in sessions while `ENABLE_ROLLER_REDEEM_WRITES=false`, lookup/payment/add-on/webhook/message gates remain closed, and no additional Live ticket is consumed. | Revisit if a fresh end-to-end button-click rehearsal is approved with exact booking, unique id, ticket id, refund/cancel plan, and a short open/close window. |
| `D0137` | 2026-07-02 | Keep the park-test full-flow window open after Sprint 2 closeout. | After T0178-T0180, Love confirmed the park test went well and asked not to close the current test locks because the app should keep working until she says otherwise. | Repository docs may close readiness/test/outcome tickets while the deployed T0176/T0177 full-flow posture remains active. Do not run a normal `park-test.json` close-window deploy unless explicitly requested. This does not broaden venue/date scope, webhooks, JumpYard-owned SMS/email sends, broad imports, or any other gates. | Revisit when Love asks to close the test window, extend scope/dates, or approves Sprint 3/4 implementation work. |
| `D0138` | 2026-07-07 | Extend the park-test full-flow operating-date window through 2026-09-30. | The park-test app should keep working for continued supervised testing after the original 2026-06-29 through 2026-07-05 window expired, and Love explicitly asked to extend the test period to the end of September. | The full-flow rehearsal config now approves Nacka venue `50871` dates from 2026-06-29 through 2026-09-30 for lookup, booking/payment, add-ons, staff auth, and redeem. This does not change venue scope, webhook processing, JumpYard-owned guest sends, broad same-day imports, or Roller/frontend architecture. | Revisit when Love asks to close the test window, shorten/extend the date window again, add venues, or move into approved Sprint 3/4 production scope. |
| `D0139` | 2026-07-07 | Existing-booking add-on prefetch may be read-only only. | Love wanted the existing-booking add-on screen to avoid a loading pause after pressing start check-in, but the system should not create a draft, payment, add-on, redemption, or other side effect before the guest intentionally continues. | The phone app may prefetch add-on availability after a successful booking lookup/session resolution and reuse that result in the add-on step. Quote, draft, payment, add-on booking, and redeem writes remain gated by the existing explicit user actions and JumpYard Cloud controls. | Revisit if backend availability reads become expensive enough to need server-side cache policy, if Roller rate limits require stricter throttling, or if product/add-on catalog ownership changes. |
| `D0140` | 2026-07-07 | Older and technically inexperienced guest support belongs in the kiosk/staff-help track. | Love confirmed that the T0184 feedback should not become another immediate phone-flow change. Guests who cannot or do not want to complete mobile/QR check-in should be helped through the later kiosk/personal-assistance setup. | T0184 closes as documentation-only. Future accessibility/support work for this audience should be scoped in the kiosk/staff-assisted track rather than adding more phone-flow guidance now. | Revisit when kiosk scope is approved or if park testing shows a critical mobile fallback gap before kiosk work starts. |
| `D0141` | 2026-07-07 | Water bottle is a server-owned phone add-on mapping with active own-bottle confirmation. | Park feedback asked guests to either buy a bottle or confirm they brought one because JumpYard does not hand out disposable cups. `Jumpy Vattenflaska` is the approved guest-facing Roller Live match. | JumpYard Cloud maps `water_bottle` to Live product `1324123`; phone/admin use the existing availability and handout contracts without frontend Roller calls. | Revisit if JumpYard changes the bottle SKU, disposable-cup policy, or product-catalog ownership. |
| `D0142` | 2026-07-07 | ComboDeal is a server-owned phone booking product mapping. | Park feedback prioritized ComboDeal product semantics, and Roller Live identifies the offer as a two-person, 60-minute, pizza-included session product with time-dependent child prices. | JumpYard Cloud maps `COMBO60` to Live parent `1318777`, counts one package as two jumpers, and selects child price ids through Live availability. | Revisit if JumpYard changes the promoted offer, needs structured pizza fulfillment, or changes product-catalog ownership. |
| `D0143` | 2026-07-09 | The active Sprint 3 queue covers phone check-in, staff/admin, and their required JumpYard Cloud work. | Love confirmed that kiosk and JumpyBoard/activity-data work are separate projects/folders and should not be mixed into the phone/admin ticket breakdown. | The current T0190-T0205 implementation queue covers phone/admin production readiness. Kiosk/print/terminal and JumpyBoard/AirHive implementation stay in their own workstreams; only explicit interface contracts may cross the boundary. | Revisit only if Love changes project ownership or approves a cross-project implementation ticket. |
| `D0144` | 2026-07-09 | Every implementation ticket must be explained in plain language before activation. | Love is not deeply technical and wants to understand what is being changed, why it is needed, and how to challenge the approach before work starts. | Each ticket explanation covers purpose, analogy where useful, inclusions/exclusions, risk/cost, dependencies, validation, and required approval. A planned backlog row is not implementation approval. | Revisit if Love asks for a different planning or approval format. |
| `D0145` | 2026-07-10 | The complete Sprint 3 target includes production booking ingestion and automatic T-30 guest messaging. | The visible phone/admin flow is not operationally complete unless JumpYard Cloud receives and refreshes Roller bookings, stores the approved normalized state, and can proactively send the secure check-in link before arrival. | Sprint 3 includes approved initial backfill, scheduled morning seed, webhook processing/reconciliation, live REST confirmation, Aurora freshness, sender/provider readiness, and one automatic SMS plus one email 30 minutes before the selected booking time. | Revisit if JumpYard removes proactive messaging, changes the timing, or chooses a different booking-index strategy. |
| `D0146` | 2026-07-10 | T0189 supersedes the T0188 ticket numbering with T0190-T0204. | The T0188 map covered sender readiness but omitted explicit production seed/backfill, webhook/reconciliation, and automatic T-30 delivery tickets. Implementation has not started, so correcting the sequence now is safer than hiding the gaps inside broad tickets. | T0189 is documentation-only. T0190 is the first implementation ticket; T0196 owns baseline/seed, T0197 owns webhook/reconciliation, T0200 owns sender readiness, T0201 owns automatic T-30 SMS/email, and T0204 proves the complete chain before GO/NO-GO. | Revisit only if Love changes the complete target or ticket boundaries before implementation. |
| `D0147` | 2026-07-10 | The park-test emergency stop is an unconditional master breaker. | The earlier model required `emergencyStop=true` while allowing smoke/full-flow flags to bypass it, so the name did not match the behavior and missing config could fail open. | Only exact runtime value `false` releases the stop; `true`, missing, or invalid blocks booking operations, park-test lookup, staff access, confirmed redeem, webhook processing, and real guest sends. Releasing the stop still requires every narrow feature/approval/allowlist/date/venue gate. Normal closed config stays `true`; reviewed active profiles use `false`. This supersedes emergency-stop bypass portions of D0120 and D0133. | Revisit only through a scoped safety design that preserves an independently testable master stop. |
| `D0148` | 2026-07-10 | Park-test venue gates require configured and observed evidence to match exactly. | The old lookup, add-on, and redeem checks rejected only known mismatches; missing booking venue could pass, and lookup could substitute configured Nacka as if Roller had supplied it. | Full-flow/assisted venue access requires a configured venue, observed booking venue, and exact `50871` match. Final Roller redeem refresh normalizes and persists venue before the second gate check. Missing configuration, missing evidence, and wrong venue all fail closed. | Revisit if Roller changes authoritative venue fields or multi-venue scope is separately approved. |
| `D0149` | 2026-07-10 | Existing park-test is Sprint 3's only Live-backed pre-production environment. | Park-test already has the separate API, Aurora, secrets, queues, schedules, observability, frontends, and Roller Live Nacka connection that a new staging foundation would duplicate. JumpYard does not need it to stay available during Sprints 3 or 4, and a second AWS stack would still share real Roller Live effects. | Keep `WRLDS:Environment=park-test`, resource prefix, names, tags, and data boundary. T0192-T0204 qualify and rehearse in that environment; no parallel staging stack is created. Park-test is never renamed or promoted into production. | Revisit if simultaneous stable/test lanes, a separate Roller tenant, different access/data policy, or stronger account isolation becomes necessary. |
| `D0150` | 2026-07-10 | T0191 revises the active phone/admin/cloud sequence to T0190-T0205. | Reusing park-test removes the staging-foundation build, while the previous T0190-T0204 sequence had no explicit ticket to create and cut over a separate production environment after GO. | T0192 qualifies/hardens park-test, T0199 prepares domains without routing production traffic, T0204 rehearses and decides GO/NO-GO in park-test, and T0205 creates/cuts over separate production only after new AWS and launch approvals. This supersedes the environment and sequence portions of D0146/T0189. | Revisit if the release model, production ownership, or final GO boundary changes. |
| `D0151` | 2026-07-13 | Every full-flow request item must use an explicitly allowed operating date before side effects. | Booking-level date checks did not prove that submitted new-booking or add-on items stayed inside the approved window, so mixed or malformed item dates could reach downstream work. | New-booking quote/draft and existing-booking add-on quote/draft validate every request item before AWS, Roller, original-booking lookup, or idempotency. Missing, malformed, mixed, and out-of-window dates fail closed; invalid gate configuration is a server error. | Revisit only with a scoped multi-date booking design that preserves an equivalent fail-closed boundary. |
| `D0152` | 2026-07-13 | The old scheduled Data API sync rule was Playground-only and had to remain disabled in Live-backed park-test until T0196. | The prior park-test schedule invoked a Lambda whose explicit Playground guard failed before any Roller token, API, or Aurora import, producing failed attempts without useful work. | T0192 disabled the unsafe schedule. T0196/D0168 supersedes this temporary posture with an exact Live/Nacka approval boundary and monitored schedule. | Revisit through D0168 rather than restoring the old broad behavior. |
| `D0153` | 2026-07-13 | Preserve the already deployed kiosk Pages origin in park-test API CORS as an interface contract. | The first T0192 diff would have removed an existing deployed origin even though kiosk implementation was explicitly outside the ticket. Preserving the established caller contract avoids an unrelated runtime regression. | All park-test profiles retain the phone, admin, and `https://jumpyard-check-in-kiosk.pages.dev` origins. This changes no kiosk code, ownership, rollout, or workstream boundary. | Revisit only in a scoped CORS/interface migration coordinated with the kiosk workstream. |
| `D0154` | 2026-07-13 | Internal and legacy API routes use AWS IAM plus their existing application token, while browser and Roller routes use explicit caller-specific application proof. | One generic Gateway lock would either block guest/staff browsers and Roller or leave operational endpoints too exposed. Gateway IAM can strongly isolate machine-only routes without replacing the service, guest, staff, and webhook checks that bind authority to the actual action. | One CDK catalog defines all 21 routes. Five session-link/send routes plus lower-level direct redeem use `AWS_IAM` and retain their service token; the other 15 routes explicitly use `NONE` at Gateway and enforce guest, staff, webhook, validation, and idempotency controls in Lambda. | Revisit if the route inventory changes materially, internal automation identity changes, or an approved staff/webhook authorizer replaces an application credential. |
| `D0155` | 2026-07-13 | Guest authority is a short-lived hash-only booking proof, and traffic protection uses aggregate route buckets rather than a shared-IP limiter. | Booking reference alone is not authority, repeated link opens must not amplify tokens/writes, and 30-40 legitimate guests can arrive together behind one park Wi-Fi address. The current HTTP API also cannot gain meaningful WAF protection through a bypassable CloudFront layer while its default endpoint remains open. | Scoped lookup/link open yields a booking-bound opaque credential held in phone memory, stored only as SHA-256, valid for at most 60 minutes, with a five-second atomic resolve cooldown and steady-state soft cap. Route limits pass the 120-guests/20-minutes model without per-IP blocking. T0199 owns domain/origin/default-endpoint topology before any edge layer; T0195 owns final token lifecycle. | Revisit with real traffic evidence, T0195 retention/serialization decisions, or T0199/T0205 edge topology. |
| `D0156` | 2026-07-14 | Ordinary park staff sign in with only a unique personal PIN; strong Cognito/TOTP identity is reserved for administrators who create, reset, enable, and disable staff records. | JumpYard expects three to five shared park devices, ordinary employees do not have managed work accounts, and authenticator-app onboarding is disproportionate for the daily staff workflow. A name, email, password, device enrollment, or MFA prompt must not be part of ordinary staff sign-in. | An administrator creates a staff record with first name, last name, and a staff-chosen six-digit PIN; normal staff receive the fixed operator role and authenticate through a hash-only opaque server session. PIN login is protected by secret-keyed lookup, slow verification, trivial-PIN rejection, source throttling, and a fail-closed venue circuit breaker. The public PIN-only model accepts a deliberate login-denial risk under sustained attack; already authenticated sessions remain independently revocable and auditable. | Revisit if JumpYard adopts workforce SSO, requires personal-device access, measures unacceptable login abuse/denial, or wants a trusted-device/network boundary. |
| `D0157` | 2026-07-14 | Cognito JWT proves administrator authentication, while Aurora remains authoritative for administrator role, venue, status, and server session state. | T0194 adds administrator routes and changes the route inventory; accepting provider claims alone would make external identity state decide JumpYard business authorization and revocation. | The deployed 26-route catalog has six `AWS_IAM`, four Cognito `JWT`, and sixteen Lambda-protected `NONE` routes. API Gateway validates JWTs on only the four `/v1/admin/*` routes; Lambda then requires a matching active Aurora `staff_admin` record and current session boundary. Ordinary staff continue to use opaque hash-only JumpYard sessions. This supersedes only D0154's 21-route inventory, not its IAM-plus-app-token machine boundary. | Revisit if administrator identity moves to workforce SSO, role ownership moves out of Aurora, or the route catalog changes materially. |
| `D0158` | 2026-07-14 | Park-test administrator passwords may use eight characters without a symbol when they include uppercase, lowercase, and a digit; TOTP remains mandatory. | Love prioritized a simpler first-admin login. The account is limited to trusted administrators, uses a separate provider flow, and still requires a second factor, while ordinary staff never see this password flow. | Cognito keeps five-password history and seven-day temporary-password validity but changes minimum length from twelve to eight and makes symbols optional. No password is set, stored, logged, or committed by infrastructure. This is an explicit usability-over-password-strength tradeoff for park-test, not production signoff. | Revisit before production, if TOTP is weakened, if more administrators are added, or when workforce SSO/passkeys become available. |
| `D0159` | 2026-07-14 | Staff/admin application views use the phone application's typography, solid-black normal copy and active icons, and mobile-first layout, while administrator credentials remain on provider-hosted Cognito Managed Login. | Three to five shared park devices need the same clear visual language and narrow-screen behavior as guest check-in. Cognito supports colors and shape settings but not a custom font family or Swedish localization; replacing the hosted credential boundary only for visual parity would materially expand authentication and security scope. | Staff root, queue, admin, and callback use the shared system-font stack and pass 320/360/390 overflow checks. Cognito uses supported black/red/radius branding without a custom logo, remains English, and retains provider-owned Open Sans. | Revisit if Cognito adds the required localization/font controls or a separate custom-auth surface is security-reviewed and approved. |
| `D0160` | 2026-07-14 | Repeated PIN login must atomically revoke the prior session before creating its replacement, and the locked transaction must revalidate the exact credential material that was verified. | The database permits only one unrevoked local-PIN session per identity, PostgreSQL does not guarantee execution order between sibling data-modifying CTEs, and an administrator reset can race the gap between slow PIN verification and transaction start. Shared-device re-login must neither collide with the unique constraint nor issue a session from stale PIN material. | PIN login serializes on the staff identity row, requires the same keyed lookup hash and scrypt verifier under that lock, then performs revoke and insert inside one Aurora Data API transaction with commit/rollback. Reset mismatch fails with the uniform invalid-PIN response; otherwise the new token becomes the only active session and the previous token is immediately rejected. | Revisit only if the one-active-session policy, credential-verification model, database engine, or session store changes. |
| `D0161` | 2026-07-14 | GitHub Issues own approved implementation scope and GitHub Projects own operational planning; shared Markdown keeps durable facts, decisions, external gates, guardrails, migration mappings, and legacy evidence only. | Manual ticket allocation and mutable queue/follow-up tables create duplicate IDs and semantic conflicts when people or agents work in parallel. GitHub supplies unique issue identity, structured ownership/status, and auditable closure through PRs. | New work starts as a Project draft, becomes a repository issue only after Love's approval, uses `codex/gh-<issue>-<slug>`, and closes through a PR containing `Closes #<issue>`. Project fields own status, priority, work type, track, and owner. `CODEX_TASK.md` is static and new work receives no manual T/FU ID; existing IDs remain legacy references. This supersedes D0093/D0094's mutable current-ticket synchronization model. | Revisit if GitHub is no longer the collaboration platform or WRLDS adopts another authoritative issue/project system. |
| `D0162` | 2026-07-14 | JumpYard Cloud uses explicit 24-hour, 30-day, 90-day, and 12-hour lifecycle limits with Roller remaining authoritative. | The operational cache needs enough state for an active visit, controlled retry, handoff, audit, and recovery, but indefinite contact, token, delivery, webhook, and staff-display retention has no approved purpose. | Expired guest tokens, idempotency rows, staff sessions, and PIN limiter rows are deleted within 24 hours of their effective window; booking/contact state is deleted or anonymized 30 days after the visit; safe pseudonymous audit/run metadata is deleted at 90 days; disabled staff display PII is anonymized at 90 days; and phone contact state has a 12-hour device-clock expiry with active cleanup and validation before reuse. Client-only code cannot execute while fully closed/offline or prove unobserved adversarial clock rollback. Raw Roller/webhook payloads, payment JWTs, tokens, PINs, secrets, and unmasked credentials remain prohibited. | Revisit only for a documented legal/contractual requirement, measured operational need, a server-trusted phone expiry gate, or stricter approved privacy policy. |
| `D0163` | 2026-07-14 | Non-dev Lambda handlers use separate least-privilege Aurora principals and retained secrets; the administrator principal is not a runtime identity. | A shared database owner credential makes every handler an administration boundary and turns one compromise into database-wide access. Handler-specific grants make the effective blast radius testable. | Existing park-test provisions one restricted role/secret per handler plus a lifecycle role after migrations. Public/broad database privileges and unused handler S3/SQS/EventBridge grants are removed; admin is limited to migrations, provisioning, and guarded recovery. A new non-dev environment requires a separately approved two-stage database/migration bootstrap before application handlers; one-shot bootstrap is prohibited. `dev` remains legacy until separately migrated. | Revisit if the runtime moves away from RDS Data API, handler boundaries materially change, or a separately approved identity platform supersedes database login roles. |
| `D0164` | 2026-07-14 | Staff PIN-pepper changes are security-driven versioned re-enrollment operations, not scheduled automatic rotation. | Replacing the pepper silently would invalidate keyed lookup/verifier material and could either lock out staff unpredictably or encourage unsafe restoration of compromised material. | A guarded `AWSPENDING` flow clears old PIN hashes, records non-secret version/re-enrollment evidence, revokes all staff sessions, then promotes the new pepper. Administrators set new PINs through the existing reset flow. Ordinary staff UX remains PIN-only, and post-invalidation containment never restores old hashes. | Revisit if the staff identity model changes, a provider supplies safe transparent key rotation, or security evidence requires a different emergency process. |
| `D0165` | 2026-07-14 | Aurora recovery rehearsals restore only into isolated, tagged, non-public resources and require database-backed lifecycle evidence before traffic. | A snapshot existing is not proof that schema, migrations, data age, lifecycle enforcement, and safe recovery are usable. Attaching an unpurged restore to the app would reintroduce expired data. | Snapshot, restore, cleanup, and snapshot deletion each require explicit approval. Rehearsals verify engine/encryption/migrations/aggregate fingerprints without PII, reapply lifecycle to the exact restored cluster, and match the safe receipt to a completed database row. Timing/data age are measurements, not a production RPO/RTO promise. | Revisit after a completed rehearsal, an approved production recovery objective, or a material Aurora architecture change. |
| `D0166` | 2026-07-14 | A pre-migration Aurora restore may test T0195 migrations only through the exact run-bound isolated migration mode; it cannot prove lifecycle apply until the dedicated lifecycle role/password has been provisioned. | The first real rehearsal showed that snapshot restore does not accept the Data API creation flag, Data API enablement is asynchronous, and a T0194 restore has no T0195 lifecycle credential. Reusing a generic source target or administrator credential for lifecycle would defeat the recovery boundary. | Recovery restores first, enables and verifies Data API, permits only exact same-run recovery from the validated cluster-without-writer stage, and derives isolated migration target identity from the external state file. The administrator secret is migration/verification-only. Full rehearsal completion remains false until a separately approved post-provisioning lifecycle apply produces database-matched evidence. | Revisit after the first complete lifecycle rehearsal or if AWS restore/Data API behavior or the database credential bootstrap changes. |
| `D0167` | 2026-07-14 | Deploy T0195 least privilege on the existing full-flow park-test profile while keeping lifecycle apply separate. | Closing the approved Nacka rehearsal window would be an unrelated behavior change, while applying purge during the credential rollout would combine two independently risky checkpoints. | Source migrations `0010`-`0012` and the 16-resource database-role delta deploy with the Nacka/date gates unchanged, webhook processing and guest sends off, and only a read-only lifecycle dry-run. Actual deletion/anonymization remains a separately reviewed apply. | Revisit after T0196 ingestion changes the data volume, before approving lifecycle apply/scheduling, or if the full-flow window closes. |
| `D0168` | 2026-07-14 | Park-test Live booking-index sync uses one-day provider windows, one request per second, reserved concurrency one, a 30-day-past-plus-all-future visit filter, and fail-closed operator resume. | Roller Live rejects Data API windows longer than one day, future visits may have been modified months earlier, shared provider capacity must be protected, and ambiguous network retries can duplicate expensive reads even when Aurora upserts are idempotent. | The 12-month discovery range is split into one-day reads; the daily 02:00 UTC job overlaps the latest success; old/unrelated rows are filtered before write; future visits remain until 30 days after visit; signed refunds are preserved; freshness is alarmed; and operator network ambiguity stops for receipt reconciliation instead of blind replay. | Revisit if Roller changes Data API limits, measured volume requires orchestration beyond one Lambda, retention changes, or production receives a separate approved schedule. |
| `D0169` | 2026-07-15 | Park-test booking webhooks are durable signals processed by one serialized SQS worker; current Roller REST state remains authoritative. | Roller retries deliveries and does not guarantee order, while the public endpoint must acknowledge quickly and the shared Live credentials must remain within the provider limit. Treating payload order or payload fields as truth could regress Aurora during duplicate or out-of-order delivery. | The exact `x-roller-apikey` token authenticates intake, which stores only normalized event metadata and enqueues before HTTP `200`. One reserved-concurrency worker verifies Live/Nacka `50871`, reads current booking detail, applies the T0196 30-day-past plus all-future boundary, then upserts normalized state. FIFO, idempotent status, five-attempt DLQ redrive, five-minute recovery, guarded replay, and live confirmation on critical actions cover duplicates, failures, missed delivery, and stale cache. | Revisit if Roller provides a signed delivery scheme, changes auth/rate limits, measured traffic requires partitioned ordering, or production receives separate credentials and rollout approval. |
| `D0170` | 2026-07-15 | Routine park-test releases build once in GitHub and promote the selected immutable artifact through a read-only plan, protected approval, OIDC deploy, exact target checks, and post-deploy verification. | Local CDK and Wrangler commands make the operator laptop part of release provenance and can rebuild or target the wrong environment. Approval is meaningful only when it follows the exact artifact and live target plan. | Pull requests run CI; eligible `main` commits produce a 90-day hashed CDK/phone/admin/migration bundle. A main-scoped read-only OIDC role renders the plan, while a separate `park-test` environment-scoped role can deploy only after approval. Rollback selects an earlier successful bundle and uses the same path without rebuilding. Cloudflare uses one protected scoped Pages token. Production stays disabled, migrations are forward-only explicit inputs, and local park-test deployment is break-glass only under separate approval. | Revisit before production, if artifact retention must exceed 90 days, if GitHub/Cloudflare identity support changes, or if measured deployment needs require another promotion platform. |
| `D0171` | 2026-07-16 | Swedish production uses `checkin.jumpyard.se` for guests and `staff-checkin.jumpyard.se` for staff/admin, with no custom API hostname in T0199. | JumpYard controls `jumpyard.se`, João received the exact records, and Love chose the two public web names while rejecting an unnecessary API name at this stage. Issue #206 later received an explicit scope extension for the minimum Cloudflare preparation needed to produce real CNAME values. | One fail-closed contract locks the two HTTPS origins, exact empty Pages projects, live-read `*.pages.dev` CNAME targets, production CORS, and staff callback/logout values. Both custom domains were associated Cloudflare-first; João applied the exact CNAMEs; public DNS is verified; and Cloudflare reports both domains active with SSL enabled. Both projects still have no Git provider and zero application deployments. The API base URL remains unset. Without a custom API hostname, the generated API endpoint must remain enabled and no non-bypassable edge claim is allowed. A later approved production Issue owns application deployment, AWS/API/CORS, and traffic. | Revisit for non-Swedish domains, an approved API custom hostname/edge topology, or the separately approved production cutover. |
| `D0172` | 2026-07-17 | Sprint 3 guest messaging ships email first; SMS is deferred, and the initial sender is `JumpYard Nacka <nackaforum@jumpyard.se>` with the same Reply-To. | Email removes the unresolved SMS sandbox, sender-id, country, and consent/provider work from the Sprint 3 critical path while still giving booked guests a direct check-in link. Replies must reach the park rather than a no-reply inbox. | T0200 defines one `jumpyard.se` SES domain identity, Easy DKIM, a dedicated fail-closed configuration set, suppression, delivery telemetry, and runtime From restriction. Dev keeps `love@wrlds.com`. Automatic/manual park-test sends remain disabled, and T0201 separately owns T-30 timing. | Revisit after the Nacka pilot, if SMS becomes necessary, if a different park sender is approved, or before multi-country rollout. |
| `D0173` | 2026-07-22 | T0200 visual-client proof uses a guarded direct SES operator for exactly two hashed/allowlisted test destinations while the application send path stays closed. | Love needed to compare Gmail and Outlook rendering without granting the session Lambda send permission, enabling a booking-time schedule, or exposing a general recipient input. | The operator requires two independent write confirmations, exact account/region/sender/DKIM/configuration-set checks, recipient-hash equality, and disabled app/schedule/IAM gates. It temporarily enables only configuration-set sending and restores it to the IaC-defined false state in `finally`. Provider ids and masked destinations are safe evidence; automatic T-30 delivery remains T0201 scope. | Revisit after manual client/header confirmation, before another controlled proof, or when T0201 reviews the application delivery path. |
| `D0174` | 2026-08-02 | The guarded T0200 operator may send to one or both of the same two hashed/allowlisted test destinations, with a recipient-count-specific confirmation phrase. | Love explicitly approved one additional Gmail-only proof of the final single-CTA design. Requiring both original destinations would send an unnecessary second message, while accepting arbitrary recipients would broaden the safety boundary. | One-address and two-address runs have different confirmations, every recipient must still match the fixed hash allowlist, all AWS/app/schedule/IAM preflight checks remain mandatory, and the configuration set must finish disabled. This does not authorize application delivery or unattended T-30 sending. | Revisit after T0200 closeout or when T0201 replaces the direct proof operator with the reviewed application delivery path. |
| `D0175` | 2026-08-03 | The first automatic T-30 proof is email-only and can target exactly one operator-agreed Roller Live booking through a hash-only booking/time/venue/recipient control tuple. | Enabling the existing broad due-booking path could contact unrelated bookings, while a recipient override would not prove real booking-contact resolution. Love requires an agreed booking at an agreed time and the email already attached to that booking. | The release scheduler runs every five minutes but sends only in the 25–30-minute-before-start window. The normal guest-send gate stays closed; SMS gets no permission. A nested object in the existing retained check-in-link secret holds only hashes and bounded control metadata, avoiding a rollback-orphaned new secret. Aurora must match once, Roller Live must confirm identifier, Nacka `50871`, date/time, active state, and settled payment, and the final SES boundary rechecks the tuple. Stable booking/start/channel idempotency makes retries at-most-once. The proof keeps the park-test Pages link; `checkin.jumpyard.se` remains separately gated. | Revisit after the single-booking proof, before removing the allowlist, before multiple recipients/parks, when changing the public check-in origin, or if delivery retry semantics change. |

| `D0176` | 2026-08-03 | General Nacka T-30 email delivery requires a separately approved, time-bounded release window; disarming the single-booking control does not authorize broad sends. | The successful #216 proof validates one real booking and provider delivery, but broad activation would contact real guests and must define when it starts, when it stops, which environment/origin is used, who monitors it, and what triggers containment. | Until Love approves the exact window and reviewed release plan, the general guest-send gate remains false and the hash-only control remains disarmed. A later rollout may include only qualifying Nacka `50871` bookings and must preserve the 25-to-30-minute window, Roller live confirmation, settled/active/contact checks, idempotency, suppression, telemetry, and emergency stop. `checkin.jumpyard.se` additionally requires its application, payment-domain, and Apple Pay readiness checks. | Revisit when Love fills in and approves the Project rollout draft, at the end of the approved window, before continued operation, or before another venue/channel. |
| `D0177` | 2026-08-03 | `checkin.jumpyard.se` may temporarily serve the selected immutable park-test phone artifact as a controlled Apple Pay test alias, without becoming a production cutover. | The final guest hostname already has active DNS/TLS and Roller has approved the payment domain, but its Pages project is intentionally empty. Love needs the working park-test app on that hostname to perform the remaining iPhone Apple Pay proof without creating a duplicate backend. | Issue #220 adds only the exact guest origin to every park-test CORS profile, keeps the phone pointed at the existing park-test API and Roller Live Nacka `50871`, leaves message links and broad guest delivery closed, and adds a protected Cloudflare-only promotion path that selects the reviewed immutable phone output. Staff/admin and production AWS/API/database remain absent. Rollback first removes CORS through an earlier immutable park-test release so the alias fails closed; Love alone performs the financial action. | Revisit after the iPhone proof, before advertising the hostname to guests, before changing message links, when closing the alias, or at the separately approved production cutover. |
| `D0178` | 2026-08-07 | The kiosk may reuse the current shared JumpYard Cloud booking boundary for ROLLER card-present payment through one explicit terminal contract. | The kiosk owns its UI and hardware flow, but deploying its copied infrastructure baseline would regress CJ's newer shared backend. ROLLER's supported contract still requires server-side draft creation, a server-owned opaque `paymentTerminal` mapping, and authoritative publish/readback after the browser package reports success. | Issue #224 adds only terminal-bound new-booking draft fields, safe attempt state, and `POST /v1/bookings/draft/finalize` to the current shared backend. The kiosk browser receives a short-lived payment JWT and safe attempt id, never credentials or the terminal reference. ROLLER remains authoritative; a browser approval cannot confirm a booking without successful ROLLER readback. Phone ecommerce behavior, kiosk UI, Android hardware, and physical payment proof remain separate. | Revisit if ROLLER replaces the package/terminal contract, the kiosk receives a separate backend, or production isolation requires a different service boundary. |
| `D0179` | 2026-08-10 | Assisted lookup may use the authenticated ROLLER API account's venue identity only when booking detail omits venue. | ROLLER Live booking detail can omit venue even for a valid Nacka booking, which made the existing fail-closed gate reject a controlled kiosk QR lookup. | Issue #230 keeps explicit booking venue authoritative. When both the provider payload and normalized booking omit venue, JumpYard Cloud calls authenticated `GET /venues/me` and accepts only an exact match with the configured Nacka venue `50871`; missing, failed, mismatched, non-Live, or explicitly conflicting evidence remains blocked. | Revisit if ROLLER starts returning booking venue consistently, changes `/venues/me`, introduces multi-venue API credentials, or before widening beyond Nacka park-test. |

## Active Constraints

| Constraint | Source | Impact | Revisit Trigger |
|---|---|---|---|
| Roller is the source of truth for bookings. | Sprint 1 project context | Booking validation must reconcile with Roller. | Revisit if JumpYard chooses another booking system. |
| JumpYard Cloud/server API owns pilot operational state. | Sprint 1 project context | Safety status, handoff code, and session status should not be owned by the frontend. | Revisit after pilot state model is finalized. |
| Dev Roller config remains Playground-only; Live use requires an explicit environment contract. | D0002/D0149/T0192 | Dev rejects non-Playground configuration. Park-test may use Roller Live only under its reviewed account, prefix, stop, feature, venue, date, and approval gates. | Revisit if the environment contract or Roller tenants change. |
| Work one approved repository Issue at a time. | D0161/WRLDS Codex workflow | Prevents implementation scope from expanding into Project drafts or future Issues. | Revisit only if workflow changes. |
| Use one dedicated issue-backed branch per approved Issue. | D0003/D0161 | Keeps Issue changes isolated from `main` and unrelated local user changes. | Revisit only if workflow changes. |
| JumpYard Cloud is the server-side integration boundary. | D0004 | Phone app work should target JumpYard Cloud contracts rather than Roller contracts. | Revisit only if architecture changes. |
| Check-in is ticket-level redemption. | D0006 | Redeem flows must resolve and operate on ticket ids. | Revisit if Roller API changes. |
| Booking index is operational cache, not source of truth. | D0009 | Seed/webhook data can speed up lookup, but live Roller state still governs check-in writes. | Revisit if data ownership changes. |
| AWS deploys require target identity preflight. | D0011/T0006 | Future `cdk deploy` runs must verify AWS account, region, environment, and WRLDS tags before changing resources. | Revisit only if deployment automation replaces local deploys. |
| Attendance is not expected-guest seed data. | D0012 | Attendance/redeem data can reconcile actual arrivals, but the morning booking index should come from bookings, tickets, payments, and customers. | Revisit only if Roller changes Data API semantics. |
| AWS dev is the implementation target after T0006. | D0013 | Future backend tickets should build against the deployed dev API/database foundation. | Revisit if dev resources are removed or replaced. |
| Aurora engine versions must match the target AWS region. | D0014 | CDK deploys should use an RDS engine version confirmed in the region, not just a generally valid Aurora version. | Revisit when upgrading database engine or changing region. |
| Aurora schema changes use versioned migrations. | D0015 | Database changes should be reviewed, committed, and applied by migration command rather than edited manually in AWS. | Revisit if migration tooling changes. |
| Playground seed writes require explicit confirmation. | D0016 | Test bookings can be useful, but writes stay local-tool-only, Playground-only, and dry-run-first. | Revisit if seed creation moves to an authenticated admin API. |
| Lookup uses live Roller detail until the booking index exists. | D0017 | T0009 can support phone lookup without pretending Aurora already has seeded booking data. | Revisit after T0011/T0012 ingestion work. |
| Phone lookup only starts check-in on ready eligibility. | D0018 | Phone UI may show unpaid bookings, but should block check-in until future payment flows handle them. Wrong-date, non-redeemable, and failed lookups still stop in lookup. | Revisit when payment and redeem flows are implemented. |
| Data API sync is modified-date based. | D0019 | Aurora ingestion needs backfill plus incremental upserts, and `bookingDate` should be treated as a returned field for local filtering, not the primary API query model. | Revisit if Roller Data API behavior changes. |
| Bookingitems import stores normalized snapshots only. | D0020/D0162 | Keeps the Aurora operational cache useful while avoiding unnecessary raw Roller payload and PII retention; visit-linked rows follow the approved 30-day lifecycle. | Revisit only if Roller contracts or the approved lifecycle materially change. |
| Product display names come from product catalog cache. | D0021 | Booking item rows can show human-readable products without storing booking free-text names or notes. | Revisit if Roller changes product catalog structure. |
| Contact PII requires a scoped ticket. | D0022 | Email and phone should be added deliberately with masking/hashing and retention rules, not as incidental raw payload storage. | Revisit in the customer/contact ingestion ticket. |
| Contact fields are structured, not raw payloads. | D0023 | Future SMS/check-in readiness can use email/phone without storing customer names, addresses, notes, or raw Roller response blobs. | Revisit before production rollout and SMS sending. |
| Webhook intake is fast-ack metadata capture first. | D0024 | Dev webhook handling dedupes and records the signal without doing slow Roller calls or snapshot mutations inside the request. | Revisit when webhook enrichment and production verification are implemented. |
| Lookup is Aurora-first but Roller-authoritative. | D0025 | Phone display lookup can use fresh local snapshots, while missing/unsafe local records still refresh from Roller before returning. | Revisit before implementing redeem or add-on booking writes. |
| Dev webhook enrichment is synchronous for now. | D0026 | Playground webhook events can update Aurora immediately, while production should move enrichment off the request path if latency or scale requires it. | Revisit before production webhook registration or higher traffic. |
| Roller Playground webhook registration is scripted and guarded. | D0027 | Dev registration can be checked and re-applied without duplicating webhooks or printing secrets. | Revisit before production registration. |
| Phone lookup metadata is non-visible by default. | D0028 | Guest UI stays clean while tests and future support tooling can verify source/freshness. | Revisit before staff/admin diagnostics. |
| Confirmed dev redeem requires token plus final refresh. | D0030 | Protected dev writes can validate Roller redemption without treating Aurora snapshot data as final authority. | Revisit before UI redeem wiring or production auth design. |
| Phone redeem handoff is server-owned. | D0031 | Phone UI can progress a check-in session, but staff/server confirmation owns final Roller redemption. | Revisit before production self-service redeem. |
| Session start is not a Roller write. | D0032 | Starting/resuming a check-in session changes JumpYard Cloud operational state only. | Revisit when session completion begins calling redeem. |
| Phone progress requires a session id. | D0033 | The phone app should not leave booking summary for a ready booking until JumpYard Cloud has created or resumed a session. | Revisit if the session model changes. |
| Phone ready-for-staff is not a Roller write. | D0034 | Completing guest safety marks JumpYard Cloud handoff state only; staff/server confirmation still owns final Roller redemption. | Revisit when implementing staff-confirmed redeem from session. |
| Staff handoff list is read-only. | D0035 | The first admin surface may inspect ready sessions, but cannot mutate sessions, call Roller, or redeem tickets. | Revisit when staff auth and final staff-confirmed redeem are scoped. |
| Staff/admin handoff uses staff auth in dev. | D0054 | The normal admin handoff list/detail/redeem flow requires a short-lived staff token issued by JumpYard Cloud. The lower-level redeem dev token remains only for controlled internal/dev direct testing. | Revisit before production rollout, role-based permissions, or Cognito/SSO implementation. |
| Staff/admin handoff UI should be mobile-first and visually aligned with the phone app. | D0055 | Staff may use personal phones for QR scanning and handoff, so the admin app must preserve tap targets, avoid horizontal overflow, and reuse JumpYard's phone-app visual language instead of becoming a separate desktop-only tool. | Revisit when a production staff design system or role-specific staff app is created. |
| QR opens JumpYard Cloud sessions only. | D0037 | Guest QR/paste flows identify the server-owned session and must not carry Roller data, secrets, or redeem authority. | Revisit when signed handoff tokens are implemented. |
| In-app booking payment must be proven before UI build. | D0038 | T0030 should verify Roller `paymentJwt`, fake/test card behavior, and PWA/domain requirements before T0031/T0032 build create-booking payment flows. | Revisit after the T0030 payment discovery spike. |
| Phone resume follows server session state. | D0039 | The phone app should use JumpYard Cloud session state after paid lookup to decide whether to continue guest flow, show QR handoff, or show already checked in. | Revisit when SMS resume and session TTL are finalized. |
| Roller payment UI needs readiness plus allowlisting. | D0040/D0056 | Server-side draft creation is confirmed and Pabel confirmed API authorization, `paymentSettings`, docs, and the Adyen card source, but phone payment UI must wait for the public test origin to be allowlisted and T0050 readiness to pass. | Revisit in T0051 after `https://jumpyard-check-in.pages.dev` is allowlisted. |
| Payment package POC stays outside phone UI. | D0041 | The POC may create a guarded Playground draft through JumpYard Cloud, but it must not introduce phone payment UI or direct frontend Roller calls until prerequisites are confirmed. | Revisit when the POC is ready for a browser payment drop-in test. |
| Phone pre-payment booking may proceed without payment drop-in. | D0042 | T0033 may build product/time selection, server-side capacity/availability, quote, draft, and payment-pending UX, but must not process payments or collect card details. | Revisit when payment integration starts. |
| Raw payment JWTs are not persisted. | D0043 | Pre-payment draft rows may store safe operational metadata and contact fields, but raw Roller payment JWTs stay response-only and out of Aurora/logs. | Revisit only after security review if server-side payment replay becomes required. |
| Raw provider payloads and credentials are not persisted. | D0162/#194 | Roller/webhook payloads, access tokens, staff PINs, secret values, and unmasked credentials stay out of Aurora, S3, logs, receipts, and CLI output. | Revisit only with a separately approved data/security decision. |
| Lifecycle apply is dry-run-first and separately authorized. | D0162/#194 | Candidate counts, reference time, cluster, limits, and digest are reviewed before a bounded transaction; migrations, live purge, and deploy are not implied by repository approval. | Revisit after the first approved live lifecycle rehearsal or if orchestration changes. |
| Non-dev runtime database access is handler-specific. | D0163/#194 | Lambda compromise should expose only the tables/operations needed by that handler; administrator and lifecycle identities are not general runtime credentials. | Revisit if the database/runtime identity architecture changes. |
| PIN-pepper promotion is fenced in Aurora before secret activation. | D0164/#194 | Re-enrollment locks and recounts the reviewed identity/session set; promotion then commits a non-secret database version fence before moving `AWSCURRENT`, so stale admin writes fail closed and an interrupted secret move remains safely retryable. | Revisit if PIN credentials leave Aurora, Secrets Manager activation becomes transactional, or the staff identity model changes. |
| Restored Aurora data cannot serve traffic before lifecycle proof. | D0165/#194 | An isolated restore may contain rows that aged out after backup creation, so lifecycle must be reapplied and the receipt matched to the restore database before any attachment. | Revisit after an approved recovery-objective decision or architecture change. |
| Add-product uses separate linked drafts. | D0044 | Existing-booking add-products create separate add-on draft bookings and link them in JumpYard Cloud; the original Roller booking is not mutated in this path. | Revisit only if JumpYard changes the add-product UX decision. |
| Add-product contact comes from the original booking. | D0078 | Existing-booking add-product drafts should reuse server-resolved original booking contact and fail closed when contact cannot be resolved, rather than asking the guest to re-enter contact details. | Revisit if Roller no longer requires customer details for separate drafts or if contact retention policy changes. |
| JumpYard-created booking contact may come from draft state. | D0080 | For linked add-product drafts, the original `prepayment_booking_drafts` row is a valid server-side contact source when the original booking was created through JumpYard Cloud. | Revisit if PII retention policy or Roller booking detail behavior changes. |
| Staff handoff PII is staff-only. | D0079 | Names and email search may be added to authenticated staff handoff to support operations, but should not be exposed in guest UI or unauthenticated APIs. | Revisit before production auth, shared-device policy, or PII retention decisions. |
| Stock-only add-ons are not check-in bookings. | D0045 | Socks-only or padlock-only add-product drafts stop at payment pending and must not be treated as redeemable guest check-in sessions. | Revisit when post-payment fulfillment is implemented. |
| Payment work resumes as T0050-T0053. | D0046/D0056/D0059 | Data API and SMS foundations have progressed, Pabel answered the key payment-prerequisite questions, and T0053 corrected the new-booking basket order before final public payment smokes. | Revisit if allowlisting is denied or the package cannot run in the PWA. |
| Frontend payment package is a narrow JWT exception. | D0057 | The phone frontend may use Roller's official payment package with the draft JWT and payment settings, but must not call general Roller REST APIs or receive Roller credentials. | Revisit after T0051 browser payment smoke. |
| New-booking payment follows basket review. | D0059 | The phone buy-entry flow should collect entry tickets and add-ons first, then create one Roller draft/payment from the combined basket. | Revisit if add-ons move to a separate payment model. |
| Gift card/Klippkort codes are payment-prep inputs. | T0102 | The phone UI should present gift card and Klippkort under payment/review, but the values must be included before quote/draft/payment because Roller applies them during booking cost and draft creation. | Revisit only if Roller supports applying these codes after draft creation. |
| SkyRider is the first capacity-gated add-on. | T0103 | The broad Roller add-on catalog includes many POS, food, drink, merch, party, event, gift-card, and membership products that should not automatically appear in the guest buy-entry flow. For the demo, only SkyRider needs availability gating because it can fail during quote/draft when Roller availability is closed or missing. | Revisit after Gustav confirms the guest-facing add-on catalog or when another add-on needs Roller capacity/session validation. |
| Prepayment draft state follows Roller settlement. | D0062 | JumpYard Cloud may store draft lifecycle state locally, but it should publish that local state only after an authoritative settled Roller booking snapshot is observed. | Revisit if Roller adds a dedicated payment webhook or draft status API. |
| Park-test pre-production and future production require readiness gates. | D0063/D0149/T0191/T0192 | Dev remains Playground. T0192 qualified park-test; T0193-T0204 own the remaining pre-production controls and rehearsal. Production stays separate until T0204 GO plus new approval. | Revisit if the environment or release model changes. |
| Daily Data API sync is an internal scheduled AWS job. | D0047 | The reconciliation job runs from EventBridge to Lambda, records run health in Aurora, and is not exposed as a public API route. | Revisit before production scheduling or larger-volume orchestration. |
| Check-in links are opaque token-based. | D0048 | SMS/deep links should resolve server-side through token hash lookup and must not rely on booking numbers alone as authority. | Revisit when production SMS, universal links, or one-time token consumption is scoped. |
| SMS sending is dry-run-first and audited. | D0049 | Server-owned SMS may create link tokens and delivery audit rows, but real sends require explicit confirmation and provider credentials stay in AWS. | Revisit before production SMS sender setup or provider switch. |
| SMS delivery needs provider diagnostics. | D0050 | `sms_sent` means AWS SNS accepted the request; CloudWatch delivery logs and SNS sandbox status determine whether a carrier delivery was possible. | Revisit when the account exits SNS SMS sandbox or provider monitoring changes. |
| SMS links resolve through JumpYard Cloud. | D0051 | The phone app should use `jy_token` only to ask JumpYard Cloud for session state, then route from that server state. | Revisit when public/mobile URL and universal-link policy are finalized. |
| Booking-time SMS sends are not enabled unattended. | D0052/D0053/D0055 | T0045 can plan or manually confirm due booking SMS sends, and T0046/T0049 can schedule planning runs, but unattended real sends remain disabled by default. | Revisit after public/mobile URL, consent, sandbox exit, and production timing are approved. |
| Confirmed scheduled SMS needs two locks. | D0055 | EventBridge confirmed sends require both the configured approval phrase and a public HTTPS check-in URL; dev stays planning-only with `confirmSend=false`. | Revisit when enabling confirmed scheduled sends. |
| Booking-time guest messaging is unified. | D0073 | SMS and email share one due-booking processor, while real unattended sends remain disabled in dev. | Revisit when enabling confirmed scheduled sends or adding channels/providers. |
| Unattended guest messaging is blocked by sender readiness. | D0075 | Dev can run controlled smoke sends, but scheduled real SMS/email must stay planning-only until SNS/SES sandbox, sender/domain, consent, and monitoring gates pass. | Revisit when enabling confirmed unattended sends. |
| Core payment/check-in flows come before production messaging unlock. | D0077 | Pabel confirmed Roller fixed the Playground card-payment issue, so the highest-risk guest flow is now payment execution, not SMS/email invitation delivery. Email/SMS are how guests enter the flow; card payment is part of the flow itself. | T0075-T0082 prioritize card payment, new booking, existing booking, add-product payment, add-product UX polish, staff redeem, data sync, and integrated flow rehearsal. SMS/email production unlock resumes after those flows are proven. |
| Park-test rollout is gated one ticket at a time. | D0097/T0144 | AWS resource creation, Roller Live reads/writes, webhook registration, payment, existing-booking lookup, catalog/index readiness, add-on, settlement reconciliation, redeem, receipt/confirmation handling, post-receipt readiness gaps, handout/payment readiness, frontend redeem rehearsal, guest contact lookup, UI/UX readiness, and assisted visitor testing must each stay behind the scoped ticket and explicit approval listed for that step. | Revisit after T0180 go/no-go or if the park-test approval model changes. |
| Park-test is separate from dev and is the sole Live-backed pre-production environment. | D0098/T0146/D0149 | Keep its existing same-account/region resources, database, secrets, API, queues, schedules, tags, and frontend targets. Do not clone or rename it; production remains a future separate target. | Revisit if concurrent lanes or stronger isolation becomes necessary. |
| Config validation separates dev and park-test before synth/deploy. | D0099/T0147 | Dev config must stay Roller Playground-only; park-test config must match the T0146 contract and keep live sends off before future synth/deploy work. | Revisit in T0148/T0152 if config or live-write gates change. |
| Raw payload bucket names must satisfy S3 length limits without replacing existing dev storage. | D0100/T0148 | New long-prefix environments can use a compact `-raw-` suffix, while deployed dev keeps its current `-raw-payloads-` bucket name. | Revisit if a future environment prefix exceeds the compact pattern. |
| First park-test deploy requires preflight and rollback readiness. | D0101/T0149 | T0150 must use the T0149 runbook, explicit deploy approval, clean dev diff, reviewed park-test template diff, and a documented rollback path before creating resources. | Revisit after T0150 or if deployment automation changes. |
| Account-wide SNS SMS diagnostics remain dev-owned. | D0102/T0150 | Park-test foundation deploy must not reconfigure SNS SMS account attributes or replace dev delivery diagnostics before a scoped messaging ticket. | Revisit when park-test SMS sending is explicitly approved. |
| Park-test sensitive runtime gates start closed and the master stop always wins. | D0103/T0152/D0147/T0190 | Normal config keeps emergency stop on and sensitive gates closed. A reviewed active profile may release the stop only alongside its exact scoped approval; no subordinate gate can bypass an active or missing stop. | Revisit only through a scoped safety design. |
| Roller Live preflight is hard-allowlisted and credential-gated. | D0104/T0153 | Live reads may use only the scoped allowlist after confirmed auth; quote, draft, webhook, payment, redeem, customer, guest, ticket, and booking endpoints remain out of scope. | Revisit when Live credentials are available or when a later ticket scopes another endpoint. |
| Live webhook registration is dry-run-first. | D0105/T0154 | T0155 must check duplicates and record a specific Live webhook id before any rollback/removal action can target it. | Revisit when T0155 registers or matches the Live webhook. |
| Live webhook processing remains closed after registration. | D0106/T0155 | Roller Live webhook `1465` may deliver to park-test, but the handler must continue returning `ignored_disabled` without Aurora inserts until processing is explicitly opened. | Revisit when enabling webhook processing in a later ticket. |
| Park-test frontend uses same source with separate deployment targets. | D0107/T0156 | Dev and park-test frontend builds differ by Cloudflare project/env var/API target, not by copied app code. | Revisit if frontend hosting or project names change. |
| First Live quote smoke uses a local guarded costs-only tool. | D0108/T0157 | The first Roller Live price check should prove `/bookings/draft/costs` and product/time selection without creating Aurora rows through the public API/Lambda path. | Revisit if a later ticket requires a deployed park-test API quote smoke before draft/payment testing. |
| Existing-booking lookup, catalog/index readiness, and add-ons are explicit park-test gates. | D0109/D0113 | Park-test must prove guest booking-code lookup, verify the Live product/add-on catalog and booking-index strategy, and then prove one existing-booking add-on path before controlled redeem or visitor testing. | Revisit if JumpYard removes existing-booking add-ons from the park-test scenario. |
| First Live draft smoke keeps public API writes closed. | D0110/T0158 | The first external Live draft may be created by local guarded tooling, while public API draft writes and Aurora state remain closed until later scoped tickets. | Revisit in T0159/T0160 when testing the actual park-test API/payment/lookup path. |
| First Live payment opens only temporary draft/payment-start gate. | D0111/T0159 | The paid booking smoke may use a reviewed payment-smoke config, but Live lookup remains T0160 and all gates are closed again after the smoke. | Revisit when T0160 opens and validates minimum Live lookup. |
| First Live lookup opens only temporary exact-identifier access. | D0112/T0160 | The controlled existing-booking lookup smoke may use a reviewed lookup-smoke config with an approval phrase and exact identifier allowlist for booking `166447399` and unique id `68b3bbb4-9a46-4379-96ac-bc7157f2fb3e`. Normal park-test config must keep Live lookup disabled, and public Live lookup output must avoid raw guest PII. | Revisit when T0161 decides lookup/index readiness, when T0162/T0163 need controlled add-on or redeem lookup behavior, or before enabling normal visitor traffic. |
| Live catalog/index readiness comes before more Live writes. | D0113/T0161 | The next park-test ticket should verify Nacka Live product/add-on IDs and decide REST-on-demand versus same-day booking-index readiness before existing-booking add-on, redeem, or visitor testing. | Revisit after T0161 documents the approved strategy. |
| First assisted park test uses REST-on-demand lookup. | D0114/T0161 | The first park test should use guest-entered booking code lookup and cache only explicitly looked-up normalized snapshots, not broad same-day booking import. | Revisit before broader or unassisted visitor traffic. |
| Existing Live booking add-ons may resolve contact through guest detail. | D0115/T0163 | T0163 proved `GET /bookings/166490323` exposes a `customerId` and `GET /guests/{customerId}` returns the complete contact required for a separate linked add-on draft, while booking detail itself may not include direct email/phone. | Revisit if Roller changes booking/guest detail response shape, removes guest detail access, or if production PII retention policy changes. |
| Linked add-on settlement must be reconciled before relying on staff handout/redeem state. | D0116/T0164 | T0164 proved the phone frontend can create and pay a separate Roller Live add-on booking for an existing booking, but local Aurora prepayment/link state remained `payment_pending` until a scoped JumpYard Cloud reconciliation observes the settled linked booking. | Revisited in T0165; keep as the gate before controlled redeem. |
| Receipt and confirmation handling must be explicit before assisted park testing. | D0117 | A park visitor may create a new booking or buy add-ons during check-in, and both cases need a clear payment-complete receipt/confirmation state for guest confidence and staff handout support. | Revisit after T0167 defines and verifies the new-booking and add-on receipt/confirmation behavior. |
| Park-test UI/UX readiness is a separate gate from technical unlocks. | D0118/D0126 | A technically successful smoke can still confuse guests or staff during the first assisted test. UI copy, recovery states, handout visibility, payment-method expectations, and staff handout details need a final device-level pass before real visitor use. | Revisit after T0178 completes UI/UX readiness or if the park-test runbook changes. |
| Linked add-on settlement updates both local records. | D0119/T0165 | A settled linked add-on must mark both `prepayment_booking_drafts` and `booking_links` as `published` after a paid Roller snapshot so staff handout state is not ambiguous. | Revisit if Roller adds a dedicated payment-settlement webhook or same-booking add-ons replace separate linked drafts. |
| Controlled Live redeem is exact-allowlist only. | D0120/T0166 | Park-test can prove Roller Live redemption through staff flow only when the reviewed config names the booking/unique id and every selected ticket id. Normal closed config keeps staff auth and redeem off. | Revisit before visitor testing or broad redeem enablement. |
| Park-test PWA receipts use Roller-native confirmations. | D0121/T0167 | The phone PWA asks Roller to send confirmation/receipt email during new-booking and add-on draft creation; JumpYard does not send separate receipt email in this ticket. The new-booking proof received Roller email; add-on proof remains unverified. | Revisit if Roller add-on delivery fails or branded JumpYard receipts are scoped. |
| Park-test readiness tickets move before frontend redeem rehearsal. | D0122/T0167 | T0168-T0173 cover add-on visibility, post-payment sync, gate naming/runbook, broader assisted lookup mode, assisted email lookup, and webhook/reconciliation readiness; T0174/T0175 now run before the T0176 frontend redeem rehearsal. | Revisit after T0176 or if park-test scope changes. |
| Live phone add-on catalog mapping is not a write unlock. | D0123/T0168 | The phone availability path may return known Nacka Live add-on ids/prices for display and quote preparation, while draft writes, existing-booking add-ons, payments, lookup, redeem, webhooks, SMS, and email remain separately gated. | Revisit if product cache import replaces static park-test mapping or if Live product ids change. |
| Park-test gate names are documented aliases. | D0124/T0170 | Human gate names in the runbook map to current CDK config keys and Lambda env vars; they do not imply the runtime variable names have been renamed in AWS. | Revisit only in a dedicated gate-name migration ticket. |
| Post-payment new-booking sync is draft-backed, not broad lookup. | D0125/T0169 | Park-test may refresh a paid new booking from Roller Live after checkout only when the lookup identifier matches a recent local `new_booking` prepayment draft created by JumpYard Cloud. This fixes the post-payment recovery path without opening general Live booking lookup. | Revisit when T0171 defines the assisted visitor lookup mode or if Roller adds a dedicated payment-settlement webhook/status API. |
| Ticket-type handout and payment-method readiness are explicit gates. | D0126/T0169 | T0169 proved checkout-to-safety technically, but ready-for-entry must show ticket duration/type for band color decisions and payment methods must be clarified because Apple Pay collapsed and Swish was not visible. | Revisit after T0175 decides the approved payment method set for the park test. |
| Assisted booking-code lookup is not a guest-list import. | D0127/T0171 | T0171 reads exactly one guest-entered booking reference or UUID, validates approved Nacka date/venue scope, and stores only the normalized looked-up snapshot. | Revisit before broader visitor rollout, email/name/staff search, or all-day booking indexing. |
| Public assisted email lookup needs confirmed API support. | D0128/T0172 | Staff can search Roller Venue Manager by email, but the PWA should not implement email lookup until Roller confirms a narrow API path. | Revisited on 2026-06-30; superseded by D0136/T0177 after current Roller REST search docs and read-only Live smoke evidence confirmed the endpoint shape. |
| First assisted park-test does not need webhook processing for confirmation. | D0129/T0173 | New booking payment, existing lookup, add-on settlement, and redeem confirmation should use scoped REST reads or direct Roller write responses; Live webhook processing remains closed. | Revisit before background automation, unassisted traffic, or production webhook processing. |
| Frontend redeem rehearsal waits for handout/payment readiness. | D0130/T0173 | T0174 ready-for-entry handout UI and T0175 payment-method readiness should run before T0176 frontend redeem rehearsal. | Revisit if the park-test sequence changes again. |
| Apple Pay should be actively unblocked before the park test, with card as fallback. | D0131/T0175 | T0175 adds the missing Apple Pay domain-association file to the phone app. Roller/Adyen still must register/approve the domain and Apple Pay still needs an iPhone smoke before it can be promised. | Revisit after the deployed association file is publicly reachable and Roller/Adyen has confirmed domain registration or a specific merchant-validation error. |
| Park-test new-booking validation uses approved parent products plus Live availability. | D0132 | Static child variation ids are too brittle for Roller Live time/price variations. Park-test payment-sync accepts only child ids returned by Roller Live availability under approved Nacka entry/family parents. | Revisit when replacing static park-test mappings with a managed Live product catalog/import. |
| Frontend redeem rehearsal does not reopen redeem writes. | D0133/T0176 | T0176 uses a separate staff-auth-only gate for allowlisted session inspection; real Roller Live redeem writes stay off. | Revisit if another exact Live redeem is approved. |
| Assisted full-flow rehearsal uses a Nacka/date-scoped gate instead of opening all park-test locks. | D0134/D0151/T0176/T0192 | Love needs to test real card purchases, POS-created booking lookup, add-ons, safety, staff admin, and redeem before/after the assisted park-test. The safe posture is one named full-flow config scoped to JumpYard Nacka Forum (`venueId=50871`) and operating dates `2026-06-29` through `2026-09-30`; every submitted quote/draft item must match that allowlist before side effects, while webhook processing, JumpYard-owned guest sends, and broad same-day imports stay closed. | Close by redeploying normal `park-test.json`; revisit before any broader visitor traffic, webhook automation, messaging, or multi-venue use. |
| Assisted lookup may store Roller display names needed for staff handoff. | D0135/D0162/T0176 | POS-created bookings must not appear as only `Gäst` when Roller returns a booking/customer display name. JumpYard Cloud may store the normalized booking/customer display name and customer id for the active visit, then must apply the approved 30-day lifecycle. | Revisit if Roller changes the booking/customer response shape or the lifecycle becomes stricter. |
| Guest contact lookup should use server-side Roller REST search. | D0136/T0177 | Current Roller REST Search for bookings documents `GET /bookings` with required search criteria, where `date` filters booking-item date and `keywords` searches booking name, ticket ids, guest email, and guest phone. A 2026-06-30 read-only Roller Live Nacka smoke found exactly one paid booking for a user-provided email using `date=2026-06-30`. | The public PWA may accept booking reference, email, or phone, but JumpYard Cloud must call Roller server-side, use the Europe/Stockholm operating date, validate Nacka/date scope from booking detail, return not-found when no same-day booking matches, choose the nearest upcoming valid same-day booking when multiple matches exist, avoid broad same-day imports, and mask PII in logs/docs. Data API remains for batch cache/index work, not real-time public lookup. | Revisit if Roller changes search semantics or rate limits, if nearest-upcoming selection causes operational confusion in test traffic, or when Aurora/Data API/webhook contact indexing becomes the primary production search path. |
| Park-test full-flow window remains open after Sprint 2 closeout. | D0137/T0180 | T0178-T0180 can be closed in docs while the deployed T0176/T0177 full-flow posture remains active until Love asks to close it. | Revisit when Love asks to close the test window, extend scope/dates, or approves Sprint 3/4 implementation work. |
| Park-test full-flow date window is extended through September. | D0138 | The deployed Nacka full-flow posture now allows operating dates `2026-06-29` through `2026-09-30` so continued park testing works, without opening webhooks, JumpYard-owned sends, broad imports, or additional venues. | Revisit when Love asks to close or change the window. |
| Live-backed park-test daily Data API sync is exact-scope only. | D0152/D0168/T0196 | The existing EventBridge rule is enabled only with the T0196 Live approval, Nacka `50871`, one-day provider requests, reserved concurrency one, retention filtering, structured receipts, and freshness alarm. | Revisit before production, another venue, a different cadence, or a material provider-limit change. |
| Park-test CORS preserves established phone, admin, and kiosk origins. | D0153/T0192 | The kiosk origin is retained only as an existing interface contract; kiosk implementation remains in its separate workstream. | Revisit through a coordinated CORS/interface migration, not an unrelated check-in ticket. |
| Machine-only API routes require two independent locks. | D0154/T0193 | Five session-link/send routes and lower-level direct redeem require AWS IAM at Gateway plus their application service token; browser/Roller routes must enforce their explicit caller class in Lambda. | Revisit if internal automation identity or the route catalog changes. |
| Guest proof and throttling must support shared park Wi-Fi. | D0155/D0162/T0193 | Booking-bound opaque proof is short-lived and hash-only stored, expires from functional use immediately, and is deleted within 24 hours after expiry; route buckets are aggregate rather than per-IP and must preserve the approved arrival-wave model. | Revisit in T0199 for edge topology or if measured traffic invalidates the model. |
| Ordinary park staff use PIN-only login. | D0156/T0194 | Three to five shared park devices expose only a six-digit personal PIN prompt; Cognito/TOTP is limited to administrators and PIN abuse protection stays server-side. | Revisit if staff access moves to personal devices, workforce SSO becomes available, or measured abuse makes the fail-closed PIN circuit unacceptable. |
| Provider identity does not decide administrator business authorization. | D0157/T0194 | Cognito JWT authenticates four admin routes; active Aurora `staff_admin` state, venue, revocation, and server session still authorize the operation. | Revisit if role ownership moves to an approved workforce identity provider. |
| Simpler park-test admin passwords still require TOTP. | D0158/T0194 | Eight characters with upper/lowercase and a digit are accepted; symbols are optional, history remains five, and no actual password belongs in source or logs. | Revisit before production or if the second-factor boundary changes. |
| Staff/admin visual parity stops at the hosted-provider boundary. | D0055/D0159/T0194 | Application views share the phone font, black/red language, and narrow-screen safeguards; Cognito Managed Login stays English in Open Sans with only supported brand settings. | Revisit if Cognito adds localization/font controls or a custom authentication UI receives separate approval. |
| Repeated PIN login atomically replaces the prior session. | D0160/T0194 | Same-identity login locks the identity row, revalidates the exact PIN lookup/verifier, and runs revoke then insert in one Aurora transaction. This blocks reset races and preserves one active local-PIN session. | Revisit only if the credential model, one-active-session policy, database, or session store changes. |
| Existing-booking add-on prefetch is read-only. | D0139/T0182 | The phone app can hide loading by prefetching availability, but it must not create drafts, payments, add-ons, redemptions, or other write side effects before explicit guest action. | Revisit if availability reads need a cache/rate-limit policy or product catalog ownership changes. |
| Older/technically inexperienced guest fallback moves to kiosk/staff help. | D0140/T0184 | Do not add more immediate phone-flow guidance for this audience; close T0184 and handle stronger fallback in the later kiosk/personal-assistance track. | Revisit when kiosk scope is approved or if park testing shows a critical mobile fallback gap. |
| Water bottle is a server-owned phone add-on mapping with active own-bottle confirmation. | D0141/T0186 | Park feedback asked guests to either buy a bottle or confirm they brought one because JumpYard does not hand out disposable cups. The Roller Live catalog contains multiple bottle/drink candidates; `Jumpy Vattenflaska` is the most guest-facing branded match for this flow. | JumpYard Cloud maps `water_bottle` to Live product `1324123` under `Merchandise`, exposes it through the existing add-on availability contract, and keeps the phone app on JumpYard Cloud rather than Roller direct calls. Revisit if JumpYard chooses a different exact bottle SKU, changes the disposable-cup policy, or moves product mapping to a managed catalog admin. |
| ComboDeal is a server-owned phone booking product mapping. | D0142/T0187 | The park-test feedback identified combo deals as a higher-priority product semantics gap than klippkort, memberships, or parties. Roller Live product copy and availability show `ComboDeal` as a two-person, 60-minute, pizza-included session product with time-dependent child price ids. | JumpYard Cloud maps `COMBO60` to Live parent `1318777`, treats each unit as two jumpers, and lets Roller availability select child price ids `1318778`, `1318779`, or `1318780` per time. Revisit if JumpYard changes the promoted combo offer, needs structured pizza fulfillment beyond product copy, or moves product mapping to a managed catalog admin. |
| Sprint 3 implementation scope is phone/admin/cloud. | D0143/T0188 | Keep kiosk/print/terminal and JumpyBoard/AirHive activity-data implementation in their separate project folders/workstreams. | Revisit only if project ownership changes or a narrow interface contract is approved. |
| Project drafts require a plain-language explanation and Love's approval before conversion to an implementation Issue. | D0144/D0161/T0188 | Love must understand the purpose, boundary, risk/cost, dependencies, and completion evidence before implementation begins. | Revisit if Love requests a different approval format. |
| Sprint 3 must prove the background booking and messaging chain, not only visible screens. | D0145/D0172/T0189/T0200 | Production readiness includes seed/backfill, webhook/reconciliation, Aurora freshness, T-30 email, phone check-in, admin redeem, monitoring, and rollback. SMS is deferred outside the Sprint 3 critical path. | Revisit if the approved product target changes. |
| Park-test email identity does not open guest delivery. | D0172/T0200/#208 | `jumpyard.se`, `JumpYard Nacka <nackaforum@jumpyard.se>`, DKIM, the dedicated SES configuration set, suppression, and telemetry may be prepared while configuration-set sending, application sends, and the booking-time schedule remain off until their explicit checkpoints pass. | Revisit after SES production access and DKIM verification for the controlled proof, then again in T0201 for unattended timing. |
| Remaining Sprint 3/production-readiness outcomes are tracked in the linked GitHub Project. | D0146/D0150/T0191/D0161/#192/#194 | Legacy IDs remain migration references; approved issue #194 owns T0195 while later Project drafts remain unapproved and Project fields own status/order. Production creation and cutover remain dependent on a rehearsed GO decision and separate approval. | Revisit only through another approved planning correction. |

## Deferred Decisions

| Decision | Why Deferred | Needed By | Owner |
|---|---|---|---|
| Multi-visit remaining-value model | T0090 confirmed `GET /customers/{customerId}/multi-passes` is the documented read endpoint and is reachable, but Josh/Joao/Pabel must clarify the apply payload or provide a Playground fixture with remaining visits. | Before multi-visit guest flow. | `Josh/Joao/Pabel` |
| Production Data API limits and tenant contract | T0196 confirmed park-test Live endpoints, pagination, and the one-day window limit; production still requires separate credential/tenant, scale, and rollout approval. | Before T0205 production integration. | `T0205` |
| Webhook production verification | T0018 confirmed Playground sends the configured token in `x-roller-apikey`, but exact production auth/signature policy and optional IP allowlisting still need approval. | Prove the policy in park-test during T0197 and reconfirm it before T0205 production registration. | `T0197/T0205` |

## Reversed Decisions

| ID | Date | Reversed Decision | Replacement Decision | Rationale |
|---|---|---|---|---|
| `D0149` | 2026-07-10 | Create a parallel staging foundation before the complete rehearsal. | Use the existing named/tagged park-test foundation as the sole Sprint 3 pre-production environment, then create separate production only after GO. | The existing foundation already provides the required AWS isolation from dev, JumpYard does not need a concurrent stable lane during Sprints 3 or 4, and a second stack would not isolate real Roller Live effects. |
| `D0093` | 2026-07-14 | Synchronize Current Ticket/Confirmed Next tables inside `REPO_CURRENT_STATE.md`. | D0161: GitHub Project fields own operational state and `REPO_CURRENT_STATE.md` describes merged-mainline facts only. | The mutable Markdown ticket tables were removed during issue #192 to eliminate duplicate status/order sources. |
| `D0094` | 2026-07-14 | Synchronize active ticket state between mutable `CODEX_TASK.md` and `REPO_CURRENT_STATE.md`. | D0161: `CODEX_TASK.md` is a static branch-to-Issue resolver and approved scope comes from `gh issue view`. | Issue-backed branches and repository Issues replace the manual ticket handoff mechanism while archive-before-delete remains valid. |

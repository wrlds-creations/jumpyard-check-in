interface:
  display_name: "Release Notes"
  short_description: "Draft customer-readable change summaries"
  default_prompt: "Use $release-notes to draft release notes from the verified release scope."

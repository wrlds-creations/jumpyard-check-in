interface:
  display_name: "GitHub CI Fix"
  short_description: "Fix CI and build failures safely"
  default_prompt: "Use $github-ci-fix to diagnose the failing GitHub Actions job and verify the scoped fix."
